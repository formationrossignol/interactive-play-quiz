# Reste à faire — Programme LMS

Date : 2026-08-15

Backlog consolidé, pur — pas de « fait », pas de récit. Pour le détail de ce
qui est déjà fait/vérifié et pourquoi, voir `VALIDATION-STATUS.md`. Chaque
item ici reste formulé exactement comme dans ce document source, pour
pouvoir s'y référer facilement.

**Progression globale : 81/85 items (95%)** — dénominateur du programme LMS
proprement dit (§01-10) ; 4 items supplémentaires listés en fin de document
sous « Réconciliation LMS ↔ systèmes pré-existants » sont explicitement hors
périmètre et non comptés ici. Fermés : §02 Inscriptions (7/7), §03
Compétences (7/7), §05 Accessibilité (6/6), §06 Parcours adaptatifs (7/7),
§07 Analytics pédagogiques (9/9), §10 Gouvernance de contenu (8/8 — workflow
de revue complet, déploiements réels, gestion des assets, diff structurel
entre versions, brand kits, modèles/blocs réutilisables, export SCORM/xAPI +
liens de preview et localisation, tous faits ; L10N-006/traduction IA
explicitement facultative dans la spec, non faite, même posture que l'IA
d'assistance de la spec 08). Quasi fermés : §08 Évaluations avancées
(8/9 — reste seulement l'IA d'assistance, non-objectif partiel), §09 Live
Q&A (9/10), §01 Devoirs/gradebook (7/8). Quasi fermé aussi : §04
Interopérabilité Enterprise (13/14 — SSO OIDC+SAML, fondation clés LTI, Deep
Linking (cours seulement), AGS (machinerie complète et testée, mais aucune
cible notable réelle aujourd'hui — voir détail), NRPS, QTI3, SCIM 2.0,
OneRoster 1.2 (import users/enrollments + export gradebook ; import
orgs/cours/classes en résolution seule, import de notes non tenté —
question d'autorité), API publique + webhooks (endpoints en lecture seule,
signature HMAC réelle) et rotation de certificat SAML avec fenêtre de
chevauchement réelle tous faits ; seul reste ouvert le provisioning
automatique d'un compte pour un `sub` jamais vu — posture délibérée
documentée dans le code, pas un gap technique).

## Dépendances qui bloquent plusieurs items à la fois

- ~~**Aucun ordonnanceur (cron/scheduler) n'existe dans ce repo.**~~
  `pg_cron` activé (`20260812020000_scheduler.sql`) : job nocturne
  `lms-daily-analytics-and-risk-signals` (3h du matin), appelle
  `run_daily_analytics_rollup()`/`generate_risk_signals()` pour chaque
  organisation, isolé par org (l'échec d'une organisation n'annule pas les
  autres). Les deux RPC contrôlaient l'admin via `auth.uid()` — sans
  signification pour un job cron sans JWT — donc chacune a été scindée en
  wrapper vérifié (signature/comportement inchangés) + fonction interne non
  vérifiée, jamais accordée à `authenticated`/`anon`. ~~Balayage planifié de
  `release_state` (06)~~ branché depuis (`20260812130000_release_state_date_and_sweep.sql`,
  voir §06) comme 3ᵉ étape du même job. ~~Rappels d'échéance J-7/J-1/retard
  (01)~~ branchés comme 4ᵉ étape (`20260813010000_assignment_due_reminders.sql`,
  voir §01). ~~Les synchronisations SCIM/OneRoster~~/~~la livraison de
  webhooks en file~~ (04) faites aussi depuis — OneRoster REST (`oneroster-sync`)
  et SCIM sont déclenchés à la demande (jeton bearer, pas un job `pg_cron`
  planifié — la spec 04 parlait de synchronisations « planifiées » côté
  fournisseur externe, pas forcément d'un ordonnanceur interne à ce dépôt) ;
  webhooks : `dispatch-webhooks` (`CRON_SECRET`, même schéma que
  `dispatch-automation-emails`), voir §04.
- ~~**Le moteur de correction de la spec 08 (`item_answer_keys` jamais lu)**~~
  `submit_assessment_response()` le lit désormais (`20260812060000_assessment_correction_engine.sql`,
  voir §08). Débloque réellement la projection journalière **item**, faite
  depuis (`20260812070000_analytics_daily_item.sql`, voir §07) — la
  psychométrie ANA-010/011/012 reste ouverte, elle a besoin d'un agrégat
  plus riche (répartition par option, quartiles de score) que ce que cette
  projection écrit. **Ne débloque pas `extra_time` (05)** : ce moteur note `assessment_items`
  (le système spec 08), pas `exam_attempts` (le système d'examen
  pré-existant, Tier-1, sur lequel `extra_time` porte réellement) — ce
  sont deux systèmes parallèles distincts, jamais réconciliés (voir
  « Réconciliation » en bas de ce document). `assessment_attempts` n'a
  d'ailleurs aucune notion de durée/limite de temps du tout.
- ~~**Aucune UI staff ne crée de sondage/priorisation/matrice** (spec 09)~~
  fait pour **sondage** (`20260812090000_live_poll_interactions.sql`, voir
  §09) — l'écran de réponse participant qui en dépendait est fait avec.
  ~~Priorisation/matrice/brainstorm/classement forcé~~ faits aussi
  (`20260813100000`, voir §09) — éditeur et écran de réponse pour les 5
  formats désormais.
- ~~**`/lti/unlinked` est un cul-de-sac réel**~~ résolu par l'UI admin LTI
  (voir §04) : enregistrement, déploiements et `link_lti_subject()`.

## 01 — Devoirs, remises et carnet de notes

- [x] UI : remise fichier/audio/vidéo + URLs signées courte durée — `20260812150000_submission_file_uploads.sql` : bucket privé `assignment-submissions` (les autres buckets du repo sont publics, celui-ci ne pouvait pas l'être), RLS `storage.objects` en double lecture indépendante (dossier `<learner_id>/...` pour l'apprenant, jointure vers `assignments.org_id` pour le staff) — le vrai verrou sur les octets, indépendant de ce que dit `submission_files`. `submit_assignment()` accepte désormais `p_files` (upload d'abord côté storage, puis attaché atomiquement à la version créée ; vérifie que le chemin appartient bien à l'appelant). Téléchargement : `createSignedUrl()` côté client, 5 min. UI apprenant (`Assignments.tsx::LearnerAssignmentRow`, sélecteur de fichier selon `response_mode`) et staff (`GradingPanel::SubmissionFilesList`)
- [x] UI : `assignment_targets` par groupe/apprenant individuel — `Assignments.tsx::AssignmentTargetsPanel`, aucune migration (RLS `assignment_targets_manage` permettait déjà l'écriture directe). Groupe : `listGroups()` du formateur (groupes personnels, comme partout ailleurs dans ce codebase — pas un rôle org). Apprenant individuel : `PersonPicker`/`searchUsernames`, plateforme entière (pas scopé à l'org — même limite déjà acceptée partout où `PersonPicker` sert à inviter dans un groupe). Pas d'invitation par e-mail : `assignment_targets.target_id` est un uuid non-nul, aucune notion de « pending » comme `share_group_members.pending_email`. Nouvelle lecture générale `listAssignmentTargets()` (l'existante `listLearnerDueOverrides()` ne filtrait que les lignes `learner` avec dérogation)
- [x] UI : échéance/aménagement dérogatoire par apprenant (`due_override`) — `20260812180000_assignment_due_override.sql`. Colonne et lecteur (`effective_assignment_due_at()`, spec 05) existaient déjà, composaient déjà avec les aménagements. Contrainte d'unicité `(assignment_id, target_type, target_id)` ajoutée (dédup préalable) pour rendre un upsert sûr — pas de nouvelle RPC, écriture directe déjà permise par `assignment_targets_manage`. UI `Assignments.tsx::DueOverridesPanel` (apprenant saisi par UUID, même convention que `StaffAccommodations`) : appliquer/effacer une dérogation par devoir déplié
- [x] UI : vue gradebook consolidée (GBK-001 à GBK-006) — `/lms/gradebook` : matrice apprenant × grade_item par session, sous-totaux par catégorie avec coefficient (`grade_items.weight`) et exclusion de la plus basse note togglable, formule exposée par total (GBK-004), export CSV/XLSX/PDF neutralisant les formules et import CSV/XLSX (GBK-006 — `import_gradebook_csv()`, `20260812080000_gradebook_csv_import.sql` : nouvelle colonne `grade_items` source_type='manual' + `grade_results`, correspondance des personnes par nom d'utilisateur côté client contre l'effectif de la session déjà chargé, prévisualisation avec statut par ligne — OK/introuvable/doublon/note hors barème —, tout-ou-rien server-side), simulation apprenant « si je reçois X » client-only dans « Mes notes » (GBK-005). **Reste** : dashboards visuels (07)
- [ ] Job serveur de scan antivirus des fichiers (`submission_files.scan_status`) — colonne prête, aucun job (les fichiers uploadés restent `pending` indéfiniment tant que ce job n'existe pas)
- [x] Connecteur antiplagiat (interface only — non-objectif V1 explicite) — `20260812220000_plagiarism_check_interface.sql` : pas de vendor (hors scope V1 assumé), colonnes `plagiarism_check_status`/`note`/`checked_by`/`checked_at` sur `submissions` + RPC `set_plagiarism_check()` (staff seulement, aucune policy d'écriture directe n'existait sur `submissions`). UI `Assignments.tsx::PlagiarismCheckControl` dans `GradingPanel` — statut + note/lien externe, saisi manuellement par le staff après vérification hors système
- [x] Notifications programmées (J-7/J-1/retard) — `20260813010000_assignment_due_reminders.sql`. `_generate_assignment_due_reminders_internal()`, 4ᵉ étape isolée de `run_scheduled_lms_analytics_jobs()` (déjà nocturne). Résout `effective_assignment_due_at()` par apprenant (pas `due_at` brut — respecte les aménagements, `no_time_limit` désactive le rappel), expansion cible session/groupe/apprenant (même CTE que la règle `overdue` de `generate_risk_signals()`), exclut qui a déjà remis. Dédup via `metadata->>'assignment_id'`+`reminder_kind` (pas de colonne dédiée sur `notifications`, contrairement à `risk_signals.status`) — un rappel par (apprenant, devoir, type) une seule fois, jamais rejoué. Catégorie `system` réutilisée (déjà celle des notifications de note publiée) — aucune UI nouvelle, `NotificationCenter`/`/notifications` existaient déjà et fonctionnent tels quels
- [x] Correction anonyme (GRD-005, moitié) — `20260813020000_anonymous_grading.sql`/`20260813030000`. Option de devoir (`assignments.policy->>'anonymous_grading'`, colonne déjà là, jamais utilisée). `list_submissions_for_grading()` masque `learner_id` (RLS sur `submissions` expose la colonne sans condition, masquage possible seulement côté fonction) ; `lift_submission_anonymity()` — seule façon de révéler, journalisée dans `submission_anonymity_lifts` (même posture que `accommodation_access_log`, aucune colonne motif — le rôle staff + la trace suffisent, même précédent). `publish_submission_grade()` renseigne enfin `submission_assessments.is_anonymous` (jamais écrit avant). UI : case à cocher à la création, `GradingPanel` affiche « Apprenant anonymisé » + bouton lever. **Reste** : double correction — aucun schéma n'existe (pas de second correcteur, pas de table de réconciliation, rien dans le modèle indicatif de la spec non plus) — chantier distinct, pas deviné ici

## 02 — Inscriptions, sessions et gestion des apprenants

- [x] UI : import CSV/XLSX avec prévisualisation/mapping/doublons (ENR-014) — `resolve_org_members_by_identifier()` (`20260812100000_enrollment_csv_import.sql`, email ou nom d'utilisateur, scopé aux membres réels de l'org via `user_org_roles` — ne devine jamais un compte pour un identifiant inconnu), boucle client sur `enroll_in_session()` déjà idempotent/atomique (pas de nouvelle RPC bulk). Prévisualisation avec statut par ligne (OK/introuvable/doublon/déjà inscrit) et rapport CSV téléchargeable (`EnrollmentImportDialog.tsx`, bouton « Importer » par session dans `Sessions.tsx`)
- [x] UI : actions en masse (déplacer, annuler, prolonger — ENR-015 partiel) — `SessionRosterPanel.tsx` (panneau « Effectif » par session dans `Sessions.tsx`), sélection multiple, motif optionnel partagé, rapport par ligne (OK/échec). Prolonger : `extend_enrollment_due_date()` (`20260812110000_enrollment_bulk_actions.sql`, premier writer de `effective_due_at` après création, audité dans `enrollment_history`). Annuler/déplacer réutilisent `transition_enrollment()`/`enroll_in_session()` tels quels (déplacer = retrait puis réinscription orchestrés côté client, pas une nouvelle primitive atomique). **Reste** : « inscrire » non dupliqué (couvert par ENR-014), « affecter un formateur » (session_trainers est déjà en écriture directe RLS, pas vraiment une action de masse sur l'effectif sélectionné), « envoyer une relance » (contenu/déclenchement pas défini, recoupe le blocage notifications programmées de 01/07)
- [x] UI : écran participant pour voir/accepter/décliner une offre de liste d'attente — bandeau « Une place s'est libérée » dans « Mes formations » (`Sessions.tsx::WaitlistOffers`), compte à rebours 48h, accepter/refuser appellent `accept_waitlist_offer()`/`decline_waitlist_offer()` directement
- [x] Auto-inscription avec règles (domaine email, code, approbation, prérequis — ENR-013) — `20260813120000_self_enrollment_rules.sql`. `course_sessions.enrollment_policy` (posé depuis 20260810150000, jamais écrit ni lu) porte désormais `{mode, email_domains?, requires_code?, prerequisite?}` — safe à exposer publiquement (lu par `course_sessions_public_read`). Le code d'invitation ne pouvait **pas** vivre dans ce jsonb : une offre publique rend `enrollment_policy` lisible par un visiteur anonyme, un code en clair dans le même payload ne protégerait rien — table dédiée `session_enrollment_codes`, staff-only en lecture comme en écriture, jamais renvoyée au client (`self_enroll_in_session()`, security definer, la compare côté serveur). `self_enroll_in_session()` compose domaine email / code / prérequis (`evaluate_rule_definition()`, une seule feuille — pas le constructeur ET/OU complet d'Automation.tsx, non exporté) puis délègue à `enroll_in_session()` existant pour l'arithmétique places/liste d'attente ; mode `approval` crée un statut `pending` résolu par `resolve_pending_enrollment()` (nouvelle RPC — `transition_enrollment()` ne revérifie jamais la capacité sur `active`, correct pour ses appelants existants mais pas pour une approbation qui peut faire déborder une session pleine). **Reste** : mode `payment` reconnu mais explicitement refusé (`payment_required_not_implemented`) — brancher un vrai paiement réutiliserait le pattern `quiz_purchases` (RPC service-role + webhook Stripe), chantier à part non deviné ici, même posture que `pool_sections_not_supported` en spec 08. UI : `SelfEnrollmentPolicyPanel.tsx` (règles + demandes en attente) et `LearnerCatalog` dans `Sessions.tsx` (catalogue + inscription)
- [x] Vue apprenant « Mes formations » complète avec dates effectives/échéances relatives recalculées (ENR-017) — `20260813130000_enrollment_effective_dates_and_completion_policy.sql`. `enrollments.effective_start_at` était déjà, depuis 20260810150000, "la date effective d'inscription" que réclame ENR-017 — rien à ajouter côté colonne pour le début, seulement le composer avec `course_sessions.mode` (`effective_enrollment_access_start_at()`) ; nouvelle colonne `course_sessions.relative_duration_days` (le mode `self_paced_relative` existait sans aucune durée pour calculer une échéance — `effective_enrollment_due_at()`, miroir de `effective_assignment_due_at()`). 4 buckets (à venir / en cours / à terminer / terminées) au lieu des 3 précédents (par statut brut) — seuil "à terminer" = échéance effective sous 7 jours ou dépassée, choix d'affichage non spécifié par ENR-017, même esprit que le seuil 2× médiane de CMP-012. `Sessions.tsx::LearnerEnrollments` rejoint désormais `course_sessions` via `myEnrollmentsDetailed()` (embed PostgREST) au lieu d'afficher `Session {id.slice(0,8)}`
- [x] Calcul de complétion versionné par politique (activités obligatoires, score, présence, durée éventuelle) — `completion_policy_sets`/`_set_versions` (même schéma versionné que `placement_threshold_sets`, spec 06) + `publish_completion_policy()`. `_compute_enrollment_completion_internal()` : activité obligatoire = une soumission au-delà de `draft` existe ; score = moyenne pondérée des `grade_results` publiés du gradebook de la session (même `grade_items.weight` que GBK-004) ; présence = jours présents / (présents+absents+retards) sur `attendance_events`, les jours excusés sortant des deux côtés du ratio ; durée = ancienneté minimale dans `effective_start_at`. Chaque critère est optionnel et indépendant — une politique publiée vide ne ferme jamais rien automatiquement. `enrollment_completion_results` snapshotte l'`enrollment_id`+`policy_version` qui a produit le résultat (même garantie de non-rétroactivité que `release_state_exemptions.threshold_set_version`, ADP-011) — une republication ne rouvre jamais une inscription déjà `completed`. Branché comme 6ᵉ étape isolée du job nocturne (`_sweep_enrollment_completion_internal()`, `run_scheduled_lms_analytics_jobs()`). UI : `CompletionPolicyPanel.tsx` (édition + recalcul à la demande) dans `Sessions.tsx`
- [x] `attendance_events` (présence) — `20260812190000_attendance_events.sql`. Aucun modèle de séance/occurrence n'existait sur `course_sessions` (une seule fenêtre `starts_at`/`ends_at`) ; unité retenue : (session, apprenant, jour), upsert via `record_attendance()` (registrar/pedago/admin ou formateur de la session — pas d'écriture directe, même posture que `enroll_in_session()`), pas de table d'historique séparée (spec explicitement « facultatif V1 »). Émet `attendance.recorded` (`emit_learning_event`). UI : `SessionAttendancePanel.tsx`, bouton « Présence » dans `Sessions.tsx`. **Ne couvrait pas** le calcul de complétion versionné — fait ci-dessus

## 03 — Compétences, résultats d'apprentissage et preuves

- [x] UI : alignement compétence ↔ devoir/critère de rubrique (CMP-010/011 partiel) — `AlignmentManager` dans `Competencies.tsx` (bouton « Aligner » par compétence), coefficient/rôle de preuve (teaching/practice/assessment)/obligatoire. Pas de nouvelle migration : RLS `competency_alignments_manage` (`for all`) autorisait déjà l'écriture directe pedago/admin. **Reste** : cours/module/leçon/question/examen/SCORM/H5P/étape de parcours (7 des 9 `target_type` restants) — aucun sélecteur org-scopé cohérent pour ces types dans le codebase actuel
- [x] UI : vue couverture programme (enseigné/pratiqué/évalué — CMP-012, CMP-021) — `ProgramCoveragePanel` dans `Competencies.tsx`, bouton « Couverture » par référentiel. Ni « cours » ni « programme » n'existe comme entité org-scopée interrogeable (cours = localStorage uniquement, `program` n'existe que comme tag texte libre sur `metric_definitions.scope`) : la vue approxime « programme » au grain `competency_framework`, seule structure réellement org-scopée déjà navigable. Enseigné/pratiqué/évalué = `competency_alignments.evidence_role` (teaching/practice/assessment), déjà posé pour CMP-010/011. « Surreprésentée » (CMP-012) n'a pas de seuil défini par la spec — heuristique retenue : plus de 2× la médiane des alignements du référentiel, calculée seulement à partir de 3 compétences alignées (évite qu'une poignée de compétences bien alignées parmi beaucoup de vides « surreprésente » trivialement une médiane quasi nulle). Pas de nouvelle migration — lecture sur RLS déjà ouverte
- [x] UI : demande de revue apprenant (CMP-018) — RLS déjà ouverte (`competency_review_requests_learner_insert` : `learner_id = auth.uid()` ; `competency_review_requests_staff` : `for all`), pas de nouvelle migration. Apprenant (`LearnerMastery`) : bouton par compétence, désactivé si une demande est déjà ouverte, message obligatoire. Staff (`ReviewRequestsPanel`) : liste des demandes ouvertes, résoudre/rejeter. Scopé aux demandes de niveau maîtrise (`evidence_id` laissé nul) — l'UI apprenant n'expose aucune preuve individuelle à revoir, seulement le niveau calculé
- [x] Écran de migration des tags existants → compétences (mapping guidé, section « Migration des tags existants » de la spec) — `TagMigrationPanel` dans `Competencies.tsx` (bouton « Migration des tags » à côté de « Nouveau référentiel »), nouvelle table `competency_tag_mappings` (`20260813110000_competency_tag_mappings.sql`, `unique(org_id, tag)`, decision `pending`/`mapped`/`ignored`). Deux contraintes réelles trouvées avant d'écrire quoi que ce soit : (1) « par organisation » ne tient pas structurellement — les tags libres vivent dans `content.data.questions[].skills` et `exams.questions_public[].skills`, ni `content` ni `exams` n'a de colonne `org_id`, seulement `user_id`/`host_id`, et la RLS sur `content` est owner-scopée. L'**inventaire** (scan des tags) est donc nécessairement limité au propre contenu du membre staff appelant (`listMyContentSkillTags`) ; ce que la nouvelle table rend réellement org-scopé, c'est la **décision** de mapping elle-même — une fois un tag mappé ou ignoré par un pedago/admin, tout le staff de l'org voit la même décision. (2) « Les anciennes tentatives produisent des preuves » (backfill de `competency_evidence` depuis les tentatives historiques) exigerait une formule de scoring que la spec ne définit jamais, et recoupe exactement le chantier « Réconciliation » déjà signalé séparément en tête de ce document — **non fait ici**, l'écran ne fait que la décision de mapping/gouvernance, pas de backfill de preuves
- [x] Méthodes d'agrégation configurables (CMP-007) — les 5 méthodes (`20260812120000_competency_aggregation_methods.sql`) : dernière preuve (inchangé), meilleure preuve (position max), moyenne pondérée (positions pondérées par `competency_alignments.weight`, sinon 1), N preuves récentes (moyenne non pondérée des N dernières), validation manuelle (`recompute_competency_mastery()` devient un no-op délibéré, seul `set_manual_mastery_level()` change le niveau). Gap réel trouvé en construisant ceci : `mastery_scales`/`mastery_scale_levels` (CMP-006) n'avaient **aucune UI d'écriture** malgré une RLS `for all` déjà ouverte — sans échelle, la méthode configurée n'avait rien à agréger. CRUD minimal ajouté (`MasteryScaleManager` dans `Competencies.tsx`) : créer l'échelle par défaut, ajouter des niveaux, choisir la méthode
- [x] Export CASE 1.1 / Open Badges (non-objectif V1 explicite mais listé comme préparation attendue) — `openBadgesExport.ts` : export Open Badges 2.0 (JSON-LD) réel, choisi plutôt que CASE 1.1 (CASE modélise un référentiel de curriculum en arbre de documents, sans notion d'assertion apprenant — impossible d'y représenter « cet apprenant a maîtrisé cette compétence » ; Open Badges a directement le split Achievement/Assertion qui correspond à `competency`/`competency_mastery`). Bouton « Badge » par compétence côté staff (`FrameworkCompetencies`, export Achievement seul) et par ligne de maîtrise côté apprenant (`LearnerMastery`, export Assertion — visible seulement à partir du niveau `mastered`/`expert`). Aucun endpoint public d'hébergement de badge n'existe dans ce codebase : les `id` utilisent `urn:uuid:<id>` (identifiant stable mais volontairement non résolvable) plutôt qu'une fausse URL `https://` qui 404rait. Identité du destinataire = hash SHA-256 salé de l'email (`IdentityObject.hashed: true`, spec OB 2.0) — l'email en clair ne quitte jamais le JSON exporté. Pas de service tiers, pas de migration — tout se calcule côté client à partir de données déjà présentes
- [x] Vue formateur groupe × compétences (CMP-020) — `TrainerGroupMatrix` dans `Competencies.tsx`, visible aux `trainer`/`pedago`/`admin` (nouveau rôle `trainer` ajouté à la vue, qui n'existait qu'en pedago/admin jusqu'ici). Groupe = `share_groups` du formateur (même modèle personnel que le ciblage de devoirs ailleurs dans ce codebase, pas un regroupement d'org). Matrice apprenant × compétence, seuil attendu configurable colorant les écarts, clic sur une cellule → preuves de l'apprenant pour cette compétence (`competency_evidence_staff_read` couvrait déjà `trainer`). Pas de nouvelle migration — lectures seules sur RLS déjà ouverte

## 04 — Interopérabilité, identité et administration Enterprise

- [x] UI admin pour créer des `lti_registrations`/`lti_deployments` et pour relier manuellement un `sub` non reconnu à un compte (`external_mappings`) — `/lms/integrations`, formulaire d'enregistrement avec les 5 champs réels (plus de guess `issuer + "/auth"`), gestion des déploiements par enregistrement, RPC `link_lti_subject()` (nouvelle migration `20260812010000_lti_admin_linking.sql`) reliant un `sub` non reconnu à un membre existant de l'org. `/lti/unlinked` a maintenant un chemin réel de résolution côté admin
- [x] Outil de diagnostic LTI (dernier lancement, erreurs, test de connexion — LTI-006) — panneau « derniers lancements » par enregistrement (succès/rejet, raison, sub, deployment) + bouton test de connexion JWKS en direct (edge function `lti-test-connection`, non persistée)
- [x] Fondation clés de signature LTI (préalable à LTI-002/003/004) — `20260821010000_lti_tool_signing_keys.sql`, corrigée avant de considérer le sujet clos : la première version de `generate_lti_tool_key()` attendait un `vault_secret_id` déjà existant, mais rien nulle part n'appelait `vault.create_secret()` — la table `lti_tool_keys` ne pouvait jamais recevoir sa première ligne. Recorrigée pour prendre la clé privée en clair (PKCS8) et chiffrer elle-même, même geste que `create_identity_client_secret()`. Nouvelle fonction `lti-generate-key` (JWT-vérifiée, admin uniquement) génère la paire RSA (`jose`) et appelle la RPC. `lti-jwks` sert le document JWKS public par enregistrement (`get_lti_tool_jwks()`), `_decrypt_lti_tool_key()` service_role uniquement. `_shared/lti-signing.ts` : `signLtiJwt()` (signe, jamais utilisé en prod avant cette session — tout le code JWT existant ne faisait que vérifier) + `fetchLtiServiceToken()` (échange `client_credentials`/`private_key_jwt`, générique sur le `scope`, réutilisable par AGS/NRPS). Testé contre vraies paires RSA (pas de mock). **Reste** : UI de rotation de clé (schéma déjà prêt, pas d'écran)
- [x] Deep Linking (LTI-002) — `20260821020000_lti_deep_linking.sql`. `_shared/lti.ts` élargi pour accepter `LtiDeepLinkingRequest` en plus de `LtiResourceLinkRequest` (additif, testé en régression : `LtiDeepLinkingResponse` reste rejeté exactement comme avant). **Vérification de réalité faite avant de coder le sélecteur** : le contenu quiz/sondage/examen de ce LMS est entièrement basé code-partie/code-rejoindre/session live (`/quiz/:gameCode`, `/join-exam/:joinCode`, `/take/:joinCode`) — aucune route directe « un apprenant, un contenu, sans session » n'existe pour ces types, exactement ce qu'un lancement resource-link LTI attend pourtant. Seul `course` a une route directe adressable par id (`/course/:courseId`) déjà. Fermé pour les cours uniquement, réellement de bout en bout — sondage/quiz/examen non tentés, ça exigerait d'abord cette route de passation solo qui n'existe pas, un chantier séparé non deviné ici. `lti-launch` fait bifurquer une requête deep-linking-request vers un nouveau sélecteur (`/lti/deep-link`, `lti_deep_linking_sessions` — même forme mono-usage/TTL que `lti_login_states`) au lieu du `target_link_uri` habituel, en réutilisant exactement le même chemin de résolution de compte (jamais d'auto-provisioning, même redirection `/lti/unlinked` si `sub` inconnu). Nouvelle fonction `lti-deep-linking-response` signe la réponse avec la clé active de l'enregistrement (échoue explicitement si aucune clé, jamais de JWT non signé) et répond en JSON `{jwt, actionUrl}` plutôt qu'en page HTML auto-soumise — cette fonction doit tourner authentifiée (`consume_lti_deep_linking_session()` vérifie `auth.uid()`), et seul `supabase.functions.invoke()` porte le JWT de l'appelant, une navigation brute ne le peut pas ; le POST réel vers la plateforme est donc construit et soumis côté client (`LtiDeepLink.tsx`). `content_items[].url` pointe vers une nouvelle route `/lti/resource-link?content_id=&content_type=` — aucun nouvel état persistant nécessaire pour qu'un lancement resource-link ultérieur y arrive : le mécanisme `target_link_uri` déjà existant de `lti-login`/`lti-launch` s'en charge tel quel
- [x] Assignment and Grade Services (LTI-004) — `20260821030000_lti_ags.sql`. Décision de modélisation tranchée (utilisateur, explicitement) : `grade_items.source_type` gagne `'lti'` ; nouvelle table `lti_resource_links` ancre (registration_id, resource_link_id) → une ligne de gradebook, stocke ce que la plateforme a annoncé dans la claim AGS `endpoint` (`lineitem`/`lineitems`/`scope`, jamais deviné). `_shared/lti.ts` étendu pour extraire les claims `resource_link`/AGS `endpoint` (additif, testé — `lti.test.ts` passe à 15 cas). `lti-launch` enregistre l'ancre à chaque lancement resource-link (`upsert_lti_resource_link()`, idempotent, best-effort — jamais bloquant pour la connexion). **Vérification de réalité refaite** (même discipline que Deep Linking) : aucun `grade_items` n'a jamais eu `course` comme source dans ce dépôt — le contenu cours produit de la *complétion* (`completion_policy_sets`), jamais un score numérique. Donc la machinerie AGS est réelle, générique, testée — mais rien aujourd'hui ne l'invoque de bout en bout, faute de contenu notable réel derrière un lancement LTI. Construite quand même en entier, pas juste esquissée : `_shared/lti-ags.ts` (`fetchLtiLineItem()`/`createLtiLineItem()`/`postLtiScore()`, appels HTTP réels signés, jamais mockés — 7 cas de test contre un vrai serveur HTTP local), `lti-create-line-item` (édge function, JWT-vérifiée admin — lit le `scoreMaximum` du line item déjà existant côté plateforme si `line_item_url` est déjà connu, sinon **crée réellement** un line item via POST sur `line_items_url` avec le label/max_points que l'appelant fournit — jamais un placeholder), `ensure_lti_grade_item()` (RPC, lie `lti_resource_links` → `grade_items`). Renvoi de note idempotent avec file de reprise : trigger `_enqueue_lti_ags_score()` sur `grade_results` (déclenché par tout chemin d'écriture existant — `publish_submission_grade()`, import CSV, etc. — sans modifier aucun d'eux), file `lti_ags_score_queue` (même forme pending/sent/failed que `automation_email_outbox`, spec 06), upsert par (grade_item_id, learner_id) — une note republiée avant le premier envoi écrase la même ligne plutôt que d'en empiler une seconde. `dispatch-lti-ags-scores` (édge function, gated `CRON_SECRET`, même schéma que `dispatch-automation-emails`) réclame chaque ligne atomiquement (`update … where status='pending'`) avant traitement — deux invocations concurrentes ne peuvent jamais renvoyer la même ligne deux fois ; `retry_count` borné (5) avant passage définitif à `failed`
- [x] Names and Role Provisioning (LTI-003) — `20260821040000_lti_nrps.sql`. Décision de modélisation (analogue à AGS, tranchée ici plutôt que devinée à moitié) : NRPS est scopé au **contexte** (le cours/classe externe), pas à un resource-link précis — plusieurs liens placés partagent un seul contexte et sa `context_memberships_url`, contrairement à AGS qui est par-lien. Nouvelle table `lti_contexts` (registration_id, context_external_id) séparée de `lti_resource_links`, exactement la table que l'investigation initiale de cette session avait trouvée absente. `_shared/lti.ts` étendu pour extraire la claim `namesroleservice` (additif, testé — `lti.test.ts` passe à 18 cas). `lti-launch` enregistre l'ancre à tout lancement portant un contexte + une claim NRPS (`upsert_lti_context()`, idempotent, best-effort). Synchronisation **toujours déclenchée par un admin**, jamais automatique/en tâche de fond (« synchronisation limitée au contexte autorisé » de la spec) — `lti-nrps-sync` (edge function, JWT-vérifiée) : `start_lti_nrps_sync()` fait la vérification admin + crée la ligne de run en un aller-retour, `fetchLtiContextMembership()` (`_shared/lti-nrps.ts`, HTTP réel jamais mocké) récupère le roster, chaque membre est résolu via `external_mappings` (même convention qu'AGS) — **jamais d'auto-provisioning** pour un membre du roster qui n'a jamais lancé Brivia (même posture INT-003/LTI-005 que partout ailleurs cette session), compté « non apparié » plutôt que silencieusement ignoré. Mapping rôle LTI→Brivia délibérément conservateur : Instructor/TeachingAssistant → trainer, Learner → learner, **tout le reste — y compris tout rôle de forme Administrator — n'accorde jamais aucun rôle** (`mapLtiRolesToBriviaRoles()`, testé explicitement sur ce cas) : un roster externe ne doit jamais pouvoir accorder `admin` Brivia (accès facturation/toutes données org) par simple synchronisation automatisée, ça resterait une décision produit délibérée, pas une inférence. Rôles écrits dans `user_org_roles` en mode additif seulement (jamais de révocation — même réserve que `_resolve_sso_roles()`). **Journal de provenance** (exigé explicitement par la spec) : `lti_nrps_sync_runs`/`lti_nrps_sync_members`, une ligne par membre par synchro (apparié ou non, rôles appliqués). UI : panneau « Répertoire des contextes (NRPS) » dans `/lms/integrations`, bouton de synchro par contexte + dernier résultat affiché. Vérification croisée de la contrainte `status` faite explicitement (leçon du bug AGS de cette même session, où `'sending'` manquait à l'enum) : ici pas d'état intermédiaire de type claim concurrent nécessaire — la synchro est une action admin synchrone unique, pas un dispatcher en tâche de fond multi-worker, donc pas la même classe de risque
- [ ] Provisioning automatique d'un compte pour un `sub` jamais vu (actuellement : jamais, par choix — voir commentaire en tête de `lti-launch/index.ts`). Dernier item ouvert de §04 — pas un travail non fait mais une décision de sécurité assumée (auto-créer un compte depuis une claim d'IdP externe non vérifiable) : réservée au porteur produit, pas devinée ici, cohérent avec la même posture déjà tenue pour SSO/NRPS ailleurs dans ce document
- [x] Handshake OIDC réel pour le SSO général (INT-001 à INT-004) — `20260815040000_sso_oidc.sql` + `sso-login`/`sso-callback`/`sso-discover-oidc` (edge functions), `_shared/oidc.ts` (vérification signature/issuer/audience/nonce, testée — `oidc.test.ts`, 7 cas, même rigueur que `_shared/lti.ts`). Authorization code + PKCE (pas le flux implicite de LTI — ici il n'y a pas de contrainte spec imposant form_post, code+PKCE est le choix le plus sûr côté navigateur). Secret client stocké **chiffré** (`supabase_vault`, nouveau dans ce repo — le vault hash-only de `integration_secrets` ne convient pas : un client_secret doit repartir en clair vers le token endpoint à chaque connexion, un hash ne le permet jamais) via `identity_client_secrets`/`create_identity_client_secret()`, déchiffrement seulement par `_decrypt_identity_client_secret()` (service_role uniquement, jamais accordée à `authenticated`). INT-002 (mode) : statut `testing` réellement gated à l'admin propriétaire (`sso-login` accepte un POST authentifié distinct du GET anonyme de production — un `<a href>` brut ne peut pas porter d'en-tête `Authorization`, d'où les deux entrées). INT-003 (liaison par sub stable, jamais par email seul) : `external_identities`, jamais d'auto-provisioning sur un sub inconnu — même posture que `lti-launch` (délibérée, documentée dans son en-tête) — redirection vers `/sso/unlinked`, résolution admin via `link_sso_subject()` (mirroring `link_lti_subject()`) + panneau diagnostic dans `/lms/integrations`. INT-004 (mapping attribut→rôle) : `identity_role_mappings` + `preview_sso_role_mapping()` (évalue un payload d'exemple collé par l'admin, n'écrit rien) + application réelle des rôles résolus (`_resolve_sso_roles()`) à chaque connexion réussie — additif seulement, ne révoque jamais un rôle qu'une règle ne matche plus (décision de reconciliation complète non tranchée, pas devinée ici). Nouvelle RPC publique `resolve_sso_connection_for_email()` (narrow : id+nom+protocole seulement, connexions actives seulement) + bouton « Se connecter avec {provider} » dans `AuthPage.tsx` quand le domaine de l'email tapé correspond — sans ça, le mode `required_for_domains` restait une donnée que rien ne lisait jamais côté écran de connexion réel, même trou que celui déjà trouvé et corrigé ailleurs dans ce programme. **Reste dans INT-002** : `required_for_domains` n'empêche pas réellement la connexion par mot de passe (juste une suggestion de bouton) — bloquer vraiment le mot de passe pour un domaine géré est un changement plus large sur `auth.ts`/le flux de connexion, pas tenté ici. `sso-discover-oidc` a un garde-fou anti-SSRF minimal (hôtes loopback/privés littéraux bloqués, HTTPS obligatoire) — pas une défense complète (pas de protection contre le DNS rebinding)
- [x] SAML 2.0 SP-initié réel (moitié restante d'INT-001, INT-003/INT-004 partagés avec OIDC) — `20260821000000_sso_saml.sql` + `saml-login`/`saml-acs`/`saml-metadata` (edge functions), `_shared/saml.ts`. Choix explicite de l'utilisateur (fait-maison plutôt que l'addon Supabase natif payant `signInWithSSO()`/SAML). Vérification de signature XML-DSig déléguée à `samlify` (wrappe `xml-crypto`) — **pas** réimplémentée à la main : les attaques par « signature wrapping » sont la faille SAML classique, seule une lib auditée est acceptable ici. Compatibilité Deno vérifiée avant tout code (smoke test réel : keypair auto-signé, aller-retour `IdentityProvider.createLoginResponse`/`ServiceProvider.parseLoginResponse` — accepté quand valide, rejeté quand trafiqué/non signé/signé par une autre clé) puis prouvée par `saml.test.ts` (7 cas, vraies certs x509 générées par `openssl` à l'exécution du test — signature/audience/fenêtre `Conditions`/`InResponseTo` chacun testé séparément, même rigueur que `oidc.test.ts`). Le certificat IdP est une clé **publique** (pas un secret) — stocké en clair dans `identity_connections.metadata`, aucune entrée vault nécessaire. Cette appli ne signe pas ses propres `AuthnRequest` et ne possède aucune clé privée SP (choix documenté dans la migration : la confiance repose sur la Response signée de l'IdP au retour, pas sur la Request). `saml_login_states` (RelayState + `InResponseTo` attendu) mirrors `sso_login_states`. Post-vérification (lookup `external_identities` → `/sso/unlinked` ou mint de session) **partagé avec OIDC** via `_shared/sso-session.ts` — extrait de `sso-callback` (refactor à comportement identique, revérifié), pas dupliqué, pour qu'un correctif futur s'applique aux deux protocoles à la fois. **Reste** : validation de schéma XSD complète (samlify refuse de tourner sans un validateur enregistré — celui-ci est un stub permissif, la vérification de signature reste réelle et testée, seule la conformité XSD au sens strict n'est pas couverte ; la lib compagnon usuelle, `samlify-node-xmllint`, dépend d'un binding natif qui ne tourne pas sous Deno). INT-005 rotation de certificat **avec fenêtre de chevauchement** non couverte côté SAML (le certificat est un champ simple, pas versionné comme `identity_client_secrets` — voir item dédié ci-dessous, resté ouvert)
- [x] Import/export QTI 3 (QTI-001 à QTI-004) — `20260821051500_qti3_import_export.sql`. Lacunes réelles confirmées absentes avant d'écrire quoi que ce soit : aucune colonne `external_id`/`license` nulle part dans le schéma d'item, aucune table de provenance dédiée QTI (contrairement à `assessment_legacy_question_links` pour l'import legacy), `prompt` sans champ media. Ajouté `assessment_items.external_id`(unique par org)/`license`. Nouvelles `qti_import_batches`/`qti_import_items` : une ligne par item QTI rencontré, importé ou non — QTI-003 exige que le rapport de rejet soit réel et persisté, pas un toast client. `import_qti_items()` mirrors `import_legacy_quiz_as_assessment()` (une transaction, RPC donnée un jsonb déjà parsé/classifié côté client) ; seuls les items `imported`/`adapted` écrivent une vraie ligne `assessment_items`, `rejected` reste dans le rapport seul. `apps/app/src/lib/lms/qti.ts` : parsing zip+XML côté client (`jszip`+`DOMParser`, mêmes libs que l'import PPTX), mapping allowlist strict interaction QTI → `item_type` réel (`choiceInteraction`→single/multiple choice, `textEntryInteraction`→short_answer, `orderInteraction`→ranking, `matchInteraction`/`hotspotInteraction`/`gapMatchInteraction`→adapté avec réserve, `extendedTextInteraction`→free_text/notation manuelle) — tout le reste rejeté avec la raison nommant le tag QTI réel, jamais requalifié en `mcq` ou autre par défaut (testé explicitement, `qti.test.ts`). **Bug trouvé par le test lui-même avant tout commit** : le filtre d'import cherchait `type` contenant la sous-chaîne « assessmentItem » — aucun vrai manifeste QTI 3 n'utilise cette chaîne (la vraie valeur IMS est `imsqti_item_xmlv3p0`, que le propre code d'export de ce fichier écrivait déjà) — les propres paquets exportés par cette appli auraient été illisibles par son propre import, et tout vrai paquet QTI aurait échoué à l'import. Export (`buildAssessmentQtiPackage`) : sections fixes avec mapping fidèle → vrai zip QTI + `imsmanifest.xml` ; sections pool (QTI n'a aucun équivalent « tirage aléatoire depuis une collection nommée ») et types sans forme QTI fidèle exclus avec raison déclarée dans le rapport d'export, jamais tus ni statifiés en faux. Round-trip `external_id` (import → export → même identifiant) vérifié par test. Lecture des clés de correction pour l'export via nouvelle `get_item_answer_keys_for_export()` (`item_answer_keys` n'a aucune policy select pour `authenticated` — lecture scopée trainer/pedago/admin de l'org de l'item, même frontière que `assessment_item_revisions_staff`). Accommodations : mésappariement quasi total confirmé (aménagements de ce LMS = dérogations d'exécution par apprenant ; vocabulaire d'accessibilité QTI = propriétés de contenu à l'auteurage) — non forcé, seul `imageAlt` est une vraie propriété de contenu exportable. UI : panneau d'import (aperçu/rapport avant confirmation, même forme que `PresentationImportDialog.tsx`) + bouton export par évaluation dans `ItemBank.tsx`. **Reste** : cloze/matching/hotspot importés en `adapted` (signalés pour revue, jamais auto-acceptés en aveugle) faute de correspondance sans perte avec le contrat `correct_answer` de cette appli — pas de test round-trip pour ces types au-delà de single_choice/short_answer/ranking
- [x] Sync SCIM 2.0 (SCM-001→004) — `20260821050000_scim.sql` + `scim-users`/`scim-groups` (edge functions, `verify_jwt=false` — un IdP externe appelle directement, aucune session Supabase). Nouveau pattern d'auth pour ce dépôt : `api_clients`/`api_tokens` existaient déjà (`20260810180000`) mais `token_hash` n'avait ni écrivain ni vérificateur — `_shared/scim-auth.ts::verifyScimBearerToken()` hash le header `Authorization: Bearer` (SHA-256, primitive correcte pour un secret haute-entropie généré machine, pas un hash de mot de passe) et le résout via `_verify_api_token()` (service_role uniquement — leçon `record_sso_login` de cette session déjà appliquée). Auto-provisioning délibérément **différent** de SSO/LTI/NRPS : SCM-001 (« créer... Users ») EST l'événement de provisioning lui-même par construction du protocole — refuser de créer un compte viderait SCIM de son sens, contrairement à un `sub` SSO non reconnu qui n'a par définition aucune confiance préétablie. Mapping rôle admin autorisé (contrairement à NRPS) : un jeton SCIM ne peut exister que parce qu'un admin d'org l'a délibérément créé — frontière de confiance plus forte qu'un rôle annoncé par une plateforme tierce non vérifiable. Format réel RFC 7643/7644 (`_shared/scim-format.ts`, testé — 9 cas) : `schemas`/`ListResponse`/`PATCH Operations`/erreur `{status,detail}`. **Bug trouvé par le test lui-même avant tout push** : `meta.location` doublait le segment de chemin (`.../scim-users/Users/u1` au lieu de `.../scim-users/u1`) — le chemin réel routé par la fonction n'a pas de préfixe `/Users/`, corrigé. SCM-003 (désactivation ≠ suppression) : `deactivate_scim_user()` ne touche que `scim_users.active`/`user_org_roles`, jamais `auth.users`/`grade_results`/certificats — vérifié par lecture du corps de la fonction (aucun harnais de test Postgres live n'existe dans ce dépôt, toute la suite `deno test` teste des fonctions pures). **Lacune assumée** : SCM-004 mappe groupe SCIM → rôle Brivia réellement, mais pas → « groupe Brivia » — `share_groups` est un modèle personnel/formateur, pas org-scopé, forcer les groupes SCIM dedans aurait été un mauvais raccord. Révocation de rôle sur retrait de groupe non implémentée (additif seulement, même réserve que SSO/NRPS). Écran admin : jetons + mapping par client dans `/lms/integrations::ApiSection`. **Divergence notée, pas résolue** : un commentaire préexistant dans `integrations.ts` suggérait `create_integration_secret()`/`integration_connections` (déjà dans `20260810180000`) comme pattern prévu pour SCIM — `api_clients`/`api_tokens` a été retenu à la place (sémantiquement plus juste pour un jeton entrant côté client API), mais les deux scaffolds existent maintenant en parallèle sans que ce choix ait été tranché formellement avant cette session
- [x] Sync OneRoster 1.2 (ROS-001 à ROS-005) — `20260821060000_oneroster.sql`. Décision de scope, énoncée plutôt que devinée : la liste CSV de la spec nomme 6 types de ressources (orgs/users/courses/classes/enrollments/grades) — bâtir la sémantique complète de création/mise à jour pour les 6 en une passe n'était pas réaliste sans deviner une décision de contenu (orgs/cours/classes = auto-créer un nouveau `content`(cours)/`course_sessions` depuis une ligne CSV, chose que ce programme a toujours refusé de deviner ailleurs — LTI/SSO/SCIM ne touchent jamais que les tables identité/effectif, jamais le contenu pédagogique). Users + enrollments ont un import CSV complet (résoudre-ou-signaler, jamais d'auto-provisioning — même posture que `resolve_org_members_by_identifier()`, spec 02) ; orgs/cours/classes résolus contre des lignes Brivia déjà existantes seulement (`course_sessions.code`) — une ligne CSV nommant une classe inconnue reste non appariée, jamais une nouvelle `course_sessions` silencieuse. Import de notes (une valeur SIS externe qui écraserait le gradebook Brivia) est une question d'autorité (qui a raison ?) non tranchée ici — ROS-005 (sortant) est construit pour de vrai plus bas, l'import de notes reste un vrai manque déclaré. **Bug de conception trouvé et corrigé avant tout test** : `has_org_role()` dépend de `auth.uid()` en interne — chaque fonction accordée à `service_role` avec un check `has_org_role` interne aurait échoué systématiquement pour un appelant service_role (pas de JWT). Séparé proprement : chemin CSV (`authenticated`, admin réel, check `has_org_role`) vs chemin REST entrant (`service_role`, fonctions `_..._service` sans check interne — l'autorisation a déjà eu lieu dans l'edge function via vérification du jeton bearer, même primitive que SCIM). ROS-002 (REST entrant) scopé aux users seulement dans cette passe : `enroll_in_session()`/`transition_enrollment()` (préexistants, non modifiés) exigent un vrai `auth.uid()` qu'un appelant service_role n'a jamais — synchro d'inscriptions par REST reste un manque déclaré, pas du code cassé silencieusement. **Deuxième bug trouvé avant commit** : résolution des emails en boucle `auth.admin.listUsers()` par ligne — lent et faux silencieusement au-delà de la pagination par défaut (50) de cette API ; remplacé par une résolution SQL unique (`_resolve_oneroster_users_internal`, jointure directe), même schéma que `resolve_org_members_by_identifier()`. `_shared/scim-auth.ts` généralisé en façade fine sur `_shared/api-token-auth.ts` (même primitive hash+vérification que ce fichier utilisait déjà, maintenant partagée avec OneRoster plutôt que dupliquée — comportement inchangé, tests SCIM existants toujours verts). ROS-003 (source/sourcedId/date de sync) : `external_mappings` déjà prévu pour `'oneroster'`, `external_id` namespacé `<org_id>:<sourcedId>` (même précaution anti-collision inter-org que `link_lti_subject()`). ROS-004 (désactivation externe ne supprime jamais) : réutilise `transition_enrollment()` vers `'withdrawn'` tel quel — le même mécanisme déjà audité que toute annulation/retrait manuel ailleurs dans l'appli, jamais un `delete`. Vérifié par lecture du corps de fonction (aucun harnais Postgres live dans ce dépôt, même limite que la vérification SCM-003). Export ROS-005 : `results.csv` au format OneRoster réel (colonnes `sourcedId`/`status`/`lineItemSourcedId`/`studentSourcedId`/`scoreStatus`/`score`/...), activation par org (`oneroster_export_settings`, même forme que `analytics_privacy_settings`), repli sur les identifiants Brivia bruts quand aucun mapping OneRoster n'existe (jamais un faux sourcedId inventé). UI : panneau OneRoster dans `/lms/integrations` (import CSV avec aperçu dry-run, historique des synchros, bascule export).
- [x] API REST publique + webhooks (API-001 à API-004) — `20260821070000_public_api_webhooks.sql`, dernière pièce de §04. **Vraie faille de conception trouvée avant tout autre code** : `webhook_endpoints.secret_hash` (`20260810180000`) ne recevait qu'un SHA-256 calculé côté client — un hash unidirectionnel peut vérifier une valeur présentée en retour, jamais servir à *calculer* une signature HMAC sur un envoi que cette appli origine elle-même (API-003 exige des webhooks signés). Corrigé avec un stockage réversible chiffré (`vault_secret_id`, même primitive que `identity_client_secrets`/`lti_tool_keys`) et une vraie RPC `create_webhook_endpoint()` (le secret est généré et affiché une fois, jamais renvoyé après) ; `secret_hash` laissé en place (inutilisé) plutôt que supprimé (colonne non réversible à retirer sans risque). Signature : `_shared/webhook-signing.ts`, HMAC-SHA256 natif (Web Crypto, aucune nouvelle dépendance npm non vérifiée) sur `${timestamp}.${rawBody}` (construction à la Stripe/GitHub — le timestamp fait partie du contenu signé, pas décoratif, ce qui permet à un récepteur de rejeter un payload rejoué), en-têtes `X-Brivia-Timestamp`/`X-Brivia-Signature`, testé (6 cas : valide, corps trafiqué, mauvais secret, timestamp trafiqué, déterminisme). File de reprise `webhook_deliveries` (pending/sending/delivered/failed) mirrors `lti_ags_score_queue` — **cross-check explicite de la contrainte statut fait avant commit** (leçon du bug AGS de cette session) : les 4 valeurs que `dispatch-webhooks` écrit réellement (`sending`/`pending`/`failed`/`delivered`) correspondent exactement à la contrainte. `dispatch-webhooks` (édge function, `CRON_SECRET`, même schéma que `dispatch-lti-ags-scores`) réclame chaque ligne atomiquement avant traitement, backoff croissant (30s×2^tentative), 6 tentatives max avant `failed` définitif. API-004 (7 événements) : `emit_webhook_event()` unique, appelé par 7 déclencheurs réels sur écriture (pas le balayage nocturne de l'automatisation, un webhook a besoin de quasi-temps-réel) — `enrollments`(insert), `submissions`(passage à submitted/late), `grade_results`(publication, même porte que le relais de score AGS), `enrollment_completion_results`(satisfied devient vrai), `certificates`(insert — pas d'`org_id` propre, repli sur le premier org du destinataire par `created_at`, même heuristique que `import_qti_items()`, approximation assumée), `assessments`(passage à `published`), `competency_mastery_history`(insert). API-002 déjà satisfait par l'infra SCIM/OneRoster de cette session (`api_clients`/`api_tokens`/`_shared/api-token-auth.ts`) — étendu avec les scopes `api:enrollments:read`/`api:grades:read`/`api:completions:read`/`api:certificates:read`. **Deuxième trou trouvé** : le formulaire d'émission de jeton envoyait toujours `scopes=[]`, et `api_clients.scopes` n'a jamais de valeur par défaut non vide nulle part — aucun jeton, y compris SCIM, n'aurait jamais pu passer un `hasApiScope()`/`hasScimScope()`. Corrigé : sélecteur de scopes réel dans `/lms/integrations`. `api-v1` (édge function, `verify_jwt=false`, vérifie son propre jeton bearer — API-002 « aucun jeton utilisateur longue durée » satisfait par construction, aucun chemin n'accepte un JWT Supabase) expose 4 ressources **lecture seule** : `/enrollments`, `/grades` (uniquement publiées), `/completions`, `/certificates` (scopé par vraie appartenance d'org via `user_org_roles`, pas l'heuristique du webhook) — pagination par curseur `(champ_date, id)` opaque, limite par org (`_check_and_log_api_request`, fenêtre glissante 120 req/min, mirrors le rate-limiter de modération spec 09, rejet 429 réel). Décision de scope, énoncée : aucune ressource écrivable via cette API — cohérent avec toute la posture §04 (LTI/SSO/SCIM/OneRoster ne touchent jamais directement les dossiers académiques) ; `api_idempotency_keys` posée pour un futur point d'entrée en écriture, non exercée ici plutôt que fabriquée pour cocher une case. OpenAPI réel (`api-v1/openapi.json`, JSON valide, servi publiquement à `/api-v1/openapi.json` sans jeton — documentation, pas une ressource protégée) décrivant exactement les 4 endpoints construits, rien d'aspirationnel.
- [x] Rotation de certificat SSO/SAML avec fenêtre de chevauchement réelle (INT-005) — `20260822010000_saml_cert_rotation.sql`. Le blocage réel : `identity_connections.metadata.idp_cert` était une chaîne unique — la remplacer était un cutover dur (l'ancien cert cesse de vérifier à l'instant où un admin écrase le champ), aucune fenêtre où l'ancien et le nouveau cert IdP sont acceptés en même temps (contrairement à `identity_client_secrets`, déjà multi-actifs pour l'OIDC côté secret client depuis `20260815040000`, exactement le même besoin réglé côté SAML ici). Nouvelle table `identity_connection_certs`, 0..N par connexion, chacun `active`/`retired` indépendamment ; `saml-acs` (edge function) essaie chaque certificat actif jusqu'à trouver le vrai signataire (signature d'abord — un échec de signature passe au suivant, tout autre rejet — audience/conditions/InResponseTo — est authoritatif dès qu'une signature a matché, pas retenté). `retire_saml_idp_cert()` refuse de retirer le dernier certificat actif restant (verrouillerait tous les utilisateurs de la connexion) — garde que `deactivate_identity_client_secret()` (OIDC) n'a pas, ajoutée ici parce que perdre le seul cert actif ferme la connexion entière, pas juste un futur renouvellement. Backfill : chaque connexion SAML existante avec `metadata.idp_cert` devient sa première ligne `active`, `saml-acs` ne peut jamais se retrouver sans certificat après la migration. UI : panneau de rotation dans `/lms/integrations` (ajouter = démarre le chevauchement, retirer = le ferme, bouton désactivé si c'est le dernier actif). Vérifié : migration + RPC + RLS rejouées contre un schéma stub reproduisant les vraies tables (Postgres jetable) — backfill correct, admin ajoute un 2ᵉ cert (2 actifs), non-admin rejeté, admin retire l'original une fois le remplacement actif (2→1), retirer le dernier actif restant refusé, retrait d'un cert déjà retiré idempotent (no-op), RLS admin voit les deux lignes / non-admin n'en voit aucune ; suite `saml.test.ts` existante (7 cas signature/audience/conditions/replay) toujours verte — `_shared/saml.ts` non modifié, seule la boucle multi-cert côté `saml-acs` est nouvelle ; `tsc`/`eslint` propres. **Non testé en conditions réelles** (pas de compte staff/IdP réel local pour dérouler une vraie rotation bout en bout), même limite que le reste du programme

## 05 — Accessibilité, inclusion et aménagements individuels

- [x] `extra_time` réel — `20260815030000_exam_extra_time_engine.sql`. Le blocage (aucun moteur de tentative chronométrée server-side) est levé : `exam_attempts.expires_at`, calculé une fois à `start_exam_attempt_atomic()` en résolvant le profil d'aménagement actif du `learner_id` (même précédence override > profil que `effective_assignment_due_at`, ACC-004), valeur `{percent}` déjà écrite par `StaffAccommodations.tsx` mais jamais lue jusqu'ici. `ExamRoom.tsx` lit `attempt.expiresAt` au lieu de recalculer son propre délai depuis `exam.durationMinutes` côté client. `save_exam_answers()` refuse toute écriture passé le délai ; `submit-exam-attempt` (edge function) force `auto-submitted` et note les dernières réponses **serveur** (pas le payload client, qui pourrait porter des réponses saisies après expiration) une fois expiré. Participants anonymes (sans `learner_id`, join par code) : comportement inchangé, aucun profil d'aménagement à résoudre pour eux. **Ne couvre pas** : affichage de l'aménagement actif avant démarrage (ACC-005) — non tenté, aurait exigé un endpoint de lecture séparé avant le premier `start_exam_attempt_atomic()`
- [x] Vérificateur d'accessibilité de contenu (A11Y-007/009/010, partiel) — `20260815020000_content_accessibility_checker.sql` : `content_accessibility_checks` était posée sans analyseur. **Prérequis trouvé avant de pouvoir vérifier quoi que ce soit** : aucun champ de texte alternatif n'existait sur une question de quiz/sondage (`BaseQuestion`/`EditableQuestion`, `questionTypes.ts`) — impossible de contrôler A11Y-007 sans d'abord donner à l'auteur un endroit où le saisir. Ajouté `imageAlt`/`imageIsDecorative` + champ dans `QuizBuilder.tsx` (visible seulement quand une image de question est présente, aucun changement pour les questions sans image). `check_content_accessibility()` (staff/propriétaire) : image sans alt ni déclaration décorative (erreur), langue de contenu absente (avertissement, A11Y-009 partiel), `drag-drop`/`hotspot` sans mode clavier connu (avertissement, A11Y-013 signalé mais pas résolu). Upsert par `(content_id, rule_code, location)` — un statut `ignored` posé par un opérateur survit aux re-vérifications, seul un « fixed » automatique referme un constat qui disparaît vraiment. UI : panneau « Vérificateur d'accessibilité » dans `/lms/accessibility`, par type de contenu. **Reste** : contraste (A11Y-009 — formes de thème trop variables selon le type de contenu pour un contrôle fiable sans deviner), blocage de publication selon politique d'organisation (A11Y-011 — la bascule `is_public` est une écriture client directe sans concept de politique par org dans ce schéma, en inventer une aurait été deviner), le texte alternatif n'est pas encore consommé par l'écran de passation réel (le champ existe et se sauvegarde, seul l'aperçu éditeur a été touché)
- [x] Socle application (A11Y-001/006/009 : focus, `prefers-reduced-motion`, contraste) — `accessibility_preferences` (high_contrast/reduce_motion) existait depuis la migration ACC mais n'était lu nulle part hors de l'écran de réglages lui-même : les interrupteurs écrivaient dans une table que rien ne consommait. `lib/accessibilityRuntime.ts` la reflète sur `<html data-a11y-contrast/data-a11y-motion>`, appliqué une fois après auth (`App.tsx`, non bloquant) et instantanément au changement (`Accessibility.tsx`). `packages/ui/components.css` (partagé app+marketing) consomme ces attributs, plus un filet `:focus-visible` global — chaque `outline: none` brut des CSS de thème a été audité avant : tous sauf ce filet avaient déjà un remplacement (box-shadow/bordure/`:focus-visible` dédié), donc ce filet ne fait que combler les trous réels, jamais écraser une règle plus spécifique. Lien d'évitement + landmark `#main-content` ajoutés dans `AppLayout.tsx` (absents jusqu'ici). **Reste** : A11Y-002/003/004/005 (navigation clavier fine par composant, ordre de tabulation, structure de titres) — non audités composant par composant, chantier qui reste ouvert ; `font_size`/`spacing`/`text_to_speech` sont dans le schéma mais `text_to_speech` seul a un interrupteur déjà câblé nulle part et `font_size`/`spacing` n'ont toujours aucun contrôle dans `PreferencesPanel`
- [x] Alternatives d'interaction accessibles (A11Y-013) — le vérificateur ci-dessus flagge `drag-drop`/`hotspot` comme « sans mode clavier connu », ce qui laissait croire à une interaction existante juste incomplète. **Vérifié avant de coder quoi que ce soit** : ce n'est pas le cas — `drag-drop`/`hotspot` (classic `questionTypes.ts`) n'ont ni éditeur dans `QuizBuilder.tsx`, ni cas de rendu dans `QuestionAnswerPanel.tsx`, ni support de notation (`supabase/functions/_shared/scoring.ts::checkAnswerCorrect` lève explicitement pour ces deux types, testé par `scoring.test.ts`), et `ExamBuilder.tsx` les exclut nommément de `EXAM_SUPPORTED_TYPES`. Ce sont des types définis mais jamais atteignables par un utilisateur, dans aucun flux — pas un problème d'accessibilité isolé mais une fonctionnalité jamais construite ; leur reconstruction (édition + rendu + notation, clavier dès le départ plutôt qu'un mode ajouté après coup) est un chantier séparé, pas tenté ici. Les deux autres types cités par la spec (« classement », « dessin ») **sont** de vraies interactions existantes : le classement (`ranking`) est déjà 100 % clavier (boutons ▲▼ dans `QuestionAnswerPanel.tsx`, jamais de drag natif) mais les boutons n'avaient pas de nom accessible (glyphe seul, WCAG 4.1.2) — `aria-label` par item + `aria-live="polite"` sur le conteneur ajoutés. Le « dessin » (`drawing`, spec 08 uniquement — n'existe pas dans `questionTypes.ts`) n'a jamais eu de canevas de dessin réel dans `TakeAssessment.tsx` : il tombe déjà dans le textarea générique de secours (`item_type` hors de la liste à cas dédiés) — déjà 100 % clavier par construction, rien à ajouter
- [x] Déclaration d'accessibilité publique (`accessibility_audits.published`) — table posée sans contenu ni écran. Écriture directe RLS déjà ouverte (`accessibility_audits_admin`, admin d'org uniquement) — pas de nouvelle RPC, juste un formulaire (périmètre/méthode/statut) + bascule publier dans `/lms/accessibility`. Lecture publique (`published = true`) volontairement sans filtre d'org (n'importe quel utilisateur connecté de la plateforme voit n'importe quelle déclaration publiée — c'est le sens de « déclaration factuelle publique », pas un vrai accès anonyme puisque la page reste dans l'appli authentifiée)
- [x] Tests automatisés (axe ou équivalent) en CI — l'infra jest-axe/`npm test` bloquant en CI existait déjà (2 tests préexistants, un seul composant couvert : Breadcrumb). Étendu aux 5 templates skeleton que CLAUDE.md impose pour tout état de chargement (`PageSkeleton`/`CardSkeleton`/`ListSkeleton`/`TableSkeleton`/`ProfileSkeleton`) et à `Button` (repos/chargement/désactivé) — rien ne protégeait ces primitives partagées avant. **Reste** : couverture axe des écrans applicatifs eux-mêmes (seuls des composants UI isolés sont testés, pas une page LMS complète en conditions réelles)

## 06 — Parcours adaptatifs, conditions et automatisations

- [x] Balayage planifié (règles à échéance temporelle) + évaluateur `date` — `20260812130000_release_state_date_and_sweep.sql`. Ordonnanceur débloqué (`pg_cron` existe depuis 07) : `_sweep_release_state_internal()` rejoue `recompute_release_state()` pour chaque apprenant actif d'une org, branché comme 3ᵉ étape isolée dans `run_scheduled_lms_analytics_jobs()` (déjà nocturne). `evaluate_rule_definition()` gère désormais `{source:"date", operator:"after"|"before", value}` — sans ce balayage, un évaluateur de date seul n'aurait jamais été réévalué (recompute ne tournait que sur événement devoir/examen/note, jamais sur le temps qui passe). UI : `Automation.tsx::RuleSets` construit soit une condition activité-terminée soit une condition date (sélecteur + date/heure).
- [x] Évaluateur `score`/`compétence` — `20260812210000_score_competency_rule_evaluators.sql`. `score` résout contre `grade_results`/`grade_items` (même convention `target_id` qu'`activity_completed`, déjà trigger-branché) en pourcentage `points/max_points`, opérateurs `gte`/`lte`. `compétence` résout contre `mastery_scale_levels.position` (comparaison cross-échelle), valeur cible = `level_code`. `recompute_release_state()` appelé directement depuis `recompute_competency_mastery()`/`set_manual_mastery_level()` (pas de trigger table dédié) pour le recompute événementiel
- [x] UI de construction en phrases « Quand [condition], alors [action] » (AND/OR, groupes) — aucune migration : `evaluate_rule_definition()` gérait déjà `{op:'and'|'or', children:[...]}` récursif et `publish_rule_set_version()` la profondeur ≤6 + détection de cycles depuis `20260810200000_adaptive_automation.sql` (ADP-003) — seule l'UI n'exposait qu'une condition. `Automation.tsx::ConditionNodeEditor`, composant récursif : racine toujours un groupe (même une condition unique sérialise en `{op:'and',children:[…]}`), boutons « + Condition »/« + Sous-groupe » par groupe, retrait par nœud (jamais le dernier enfant d'un groupe)
- [x] Simulation « voir comme cet apprenant » / dry-run avant publication (ADP-008, AUT-004) — `20260813040000_rule_definition_simulation.sql`. `evaluate_rule_definition()` prenait déjà exactement `(definition, learner_id)` mais n'était jamais accordée à `authenticated` (appelée seulement en interne) — l'exposer telle quelle aurait permis de sonder les notes/compétences d'un apprenant arbitraire via une feuille `score`/`compétence` sans contrôle d'org. `simulate_rule_definition()` l'enveloppe : `pedago`/`admin` seulement, apprenant vérifié membre de l'org. Prend la définition en paramètre (pas la version publiée d'un `rule_set`) — c'est ça le « dry-run » : teste le brouillon en cours d'édition dans `ConditionNodeEditor` avant tout `publish_rule_set_version()`. UI : champ UUID apprenant + bouton « Simuler » par règle, badge Débloqué/Verrouillé
- [x] Moteur d'exécution des automatisations (AUT-001 à AUT-005) — `20260813070000_automation_execution_engine.sql`. `automation_rule_versions` n'avait aucun writer, `record_automation_run()` aucun appelant : le moteur déclencheur→action était entièrement dormant malgré le schéma déjà posé. `publish_automation_rule_version()` (nouveau writer, valide `action_type`). `_execute_automation_action()` : 6 types AUT-002 — `notification` (table `notifications`), `email` (mise en file `automation_email_outbox`, pas d'appel direct depuis Postgres — aucun `pg_net` nulle part dans ce dépôt pour appeler une edge function depuis une migration sans pouvoir le tester en aveugle contre le projet réel ; vidée par la nouvelle edge function `dispatch-automation-emails`, même Resend déjà câblé ailleurs — pas un nouveau vendor — planification externe à configurer une fois, `x-cron-secret`), `assign_content`/`extend_due_date` (`assignment_targets`, déjà là), `add_to_group`/`remove_from_group` (`share_group_members`), `follow_up_task` (le pendant automatique de la création manuelle ci-dessous). Détection des 8 `trigger_type` : réutilise des signaux déjà calculés plutôt que d'en inventer — `enrollment`/`completion` via `learning_events`/`grade_results`, `due_soon`/`overdue` via la même expansion de cible + `effective_assignment_due_at()` que les rappels J-7/J-1, `inactivity`/`failure` via `risk_signals` nouvellement ouverts, `mastery_gained`/`mastery_expired` via `competency_mastery_history` (comparaison texte `to_level`/`from_level`, pas de lookup de position — simplification documentée). Idempotent via `automation_runs.idempotency_key`. Branché comme 5ᵉ étape isolée de `run_scheduled_lms_analytics_jobs()`. UI `Automation.tsx::AutomationRules` étendue : sélecteur de type d'action + champs par type, bouton Publier
- [x] `follow_up_tasks` — création manuelle (`20260813050000`) **et** déclenchement automatisé (`20260813070000`, action `follow_up_task` du moteur ci-dessus)
- [x] Test de positionnement / remédiation (ADP-009/010/011) — `20260813080000_placement_thresholds.sql`. Non-objectif V1 explicite (« algorithme adaptatif auto-apprenant sans règle humaine ») + le propre libellé d'ADP-009 (« seuils versionnés ») confirment : pas de branchement adaptatif mi-test à inventer, un test de positionnement est un `assessment` ordinaire (spec 08, déjà là) — seul le résultat de fin de tentative (recommander/imposer/dispenser) était nouveau. Aucune table nommée dans le modèle indicatif de la spec pour ça — versionné comme `rule_sets`/`rule_set_versions` (`placement_threshold_sets`/`_set_versions`, pas de statut mutable par ligne). Livraison réutilise l'existant : recommander/imposer → `assignment_targets` (spec 01) ; « imposer » un vrai verrou se compose avec le DSL déjà construit (condition `activity_completed` sur le devoir de remédiation), pas de nouveau mécanisme. Dispenser → nouvel effet `release_state.effect = 'exempted'` + `release_state_exemptions` (preuve auditée : tentative, score, version des seuils — ADP-011, même posture que `accommodation_access_log`). `recompute_release_state()` corrigé pour ne jamais écraser une exemption (`on conflict ... where release_state.effect is distinct from 'exempted'`) — une exemption est une dérogation délibérée, pas un état que le balayage nocturne doit reconquérir. Évalué automatiquement à `submit_assessment_attempt()` via `_apply_placement_outcome()`. UI `ItemBank.tsx::PlacementThresholdsPanel` par évaluation. **Reste** : la distinction « pas équivalente à une complétion normale » dans les tableaux de bord analytics — réelle au niveau donnée (table d'audit séparée), aucune visualisation ne la fait encore ressortir (spec 07, pas tenté ici)

## 07 — Analytics pédagogiques, psychométrie et signaux de risque

- [x] Projection journalière **item** — `analytics_daily_item` + `analytics_item_psychometrics` (`20260813170000_spec07_analytics_completion.sql`) : réponses, omissions, durée médiane, difficulté, discrimination, répartition des options et avertissements avec seuil de cohorte.
- [x] Projection journalière **programme** — `analytics_daily_program` agrège les sessions au grain `course_offering` (programme/catalogue dans le modèle actuel) et expose `get_daily_program_totals()` avec suppression sous seuil. **Corrigé cette passe** : `listDailyProgram()` existait côté lib sans aucun appelant — `get_daily_program_totals()` n'était affiché nulle part dans `/lms/analytics`. Ajouté `ProgramPanel` (tableau jour/actifs/démarrées/terminées/abandonnées/liste d'attente)
- [x] Dashboard formateur/pédagogue/admin et apprenant (ANA-005 à ANA-008) — RPC `get_my_learning_analytics()` pour la vue apprenant, dashboard staff `/lms/analytics` conservé avec seuils de confidentialité.
- [x] Analyse d'items / psychométrie (ANA-010/011/012) — quartiles de performance, distracteurs agrégés, difficulté/discrimination et codes d'avertissement dans `analytics_item_psychometrics`, lecture via `get_item_psychometrics()`.
- [x] Temps médian de réponse par item (ANA-009) — `assessment_responses.duration_ms` nullable et calcul `percentile_cont(0.5)` pour préserver les tentatives historiques. **Corrigé cette passe** : la colonne existait et le calcul la lisait, mais **rien ne l'écrivait jamais** — `submit_assessment_response()`/`submit_assessment_response_auto()` n'avaient aucun paramètre durée, `median_response_time_ms` était garanti `null` en permanence malgré la case cochée. `20260815010000_assessment_response_duration_wiring.sql` ajoute `p_duration_ms` aux deux RPC (overloads à 2 arguments supprimées plutôt que laissées comme piège) ; `TakeAssessment.tsx` mesure le temps écoulé depuis le montage de l'item (tous les items s'affichent en même temps, navigation libre — proxy documenté comme approximatif, pas une mesure de concentration réelle)
- [x] Programmation de rapports (`report_schedules`/`report_runs`) — exécuteur idempotent `_run_due_analytics_reports_internal()` branché au job `pg_cron`, snapshots agrégés et pseudonymisés. **Corrigé cette passe** : `schedule_saved_report()` n'avait aucun appelant — un rapport enregistré ne pouvait jamais être programmé, l'exécuteur ne trouvait donc jamais rien à exécuter malgré le libellé du vide `/lms/analytics` (« … et les programmer »). Ajouté `ReportScheduleControl` par rapport (fréquence + bouton). Même défaut trouvé sur `relaunch_risk_signal()` (spec 06/07, panneau Signaux de risque) — bouton « Relancer » ajouté à côté de « Créer une tâche de suivi »/« Marquer traité »
- [x] Export CSV/XLSX/PDF avec pseudonymisation — actions d'export du dashboard (`analyticsExport.ts`), aucune identité apprenant exportée.
- [x] Seuil minimal anti-réidentification sur les comparaisons de cohortes (ANA-020) — `20260812170000_analytics_privacy_threshold.sql` : la vraie faille trouvée n'était pas juste « pas de comparaison de cohortes » mais que `analytics_daily_enrollment`/`competency`/`item` étaient déjà lisibles ligne à ligne (par session/compétence/item, petit N) par tout staff via PostgREST, le client ne faisant que sommer après coup. Policies de lecture directe supprimées, remplacées par 3 RPC `security definer` agrégées à org+jour, qui suppriment la période entière sous un seuil configurable par org (`analytics_privacy_settings`, défaut 5, géré par `pedago`/`admin` via un panneau « Confidentialité » sur `/lms/analytics`). Ne construit pas les écrans de comparaison de cohortes eux-mêmes (ANA-007, toujours inexistants) — ferme la fuite réelle et pose le mécanisme de seuil que ces écrans (et ANA-011) réutiliseront
- [x] Ordonnanceur réel pour `run_daily_analytics_rollup()`/`generate_risk_signals()` — `pg_cron`, job nocturne par organisation (voir dépendances en tête de document)

## 08 — Évaluations avancées et banque d'items versionnée

- [x] Assemblage réel d'une évaluation — sections fixes et **tirage pool aléatoire** : `/lms/item-bank`, règles collection + nombre d'items, publication snapshotte les règles, `start_assessment_attempt()` fige un tirage par tentative et le reprend sans redessiner
- [x] Barèmes riches (ASM-012) — **pour 4 types sur 21** (`true_false`/`single_choice`/`mcq`/`short_answer`, les seuls avec UI d'auteur) : points fixes, crédit partiel + pénalité par option fausse (mcq), équivalences insensibles casse/espaces (short_answer). Tolérance numérique non couverte (aucun type numérique n'a d'UI). **Reste** : ranking/matching/cloze et les 8 types ASM-017-024
- [x] Simulation de barème avant publication (ASM-013) — `20260812200000_assessment_scoring_simulation.sql` : `item_answer_keys` n'a aucune policy select pour `authenticated` (même staff), donc RPC serveur `simulate_item_scoring()` réutilisant `_score_assessment_response()` telle quelle (jamais de dérive avec la vraie correction), retourne seulement le résultat (correct/points), jamais la clé. UI `ItemBank.tsx::SimulateForm`, bouton « Simuler » par révision — apte pour les 4 types notables
- [x] Moteur de correction réel utilisant `item_answer_keys` — **la pièce la plus bloquante du programme** : `submit_assessment_response()` lit `item_answer_keys` pour la première fois dans ce repo, corrige côté serveur, jamais de fuite de la réponse correcte au client. Contrat JSON `correct_answer`/`scoring_rules` défini et documenté (`20260812060000_assessment_correction_engine.sql`). Testé fonctionnellement (11 cas : crédit partiel + plancher à 0, équivalences, casse) en transaction annulée avant commit, puis déployé en prod. **Non testé en conditions réelles** (pas de compte staff/apprenant local pour dérouler un cycle complet création→passation→note)
- [x] Nouveaux types d'interaction — éditeur de banque et lecteur de réponse couvrent ranking/matching/cloze, labeling, vidéo/audio, dessin, math/graphique, fichier et code ; les formats sans comparateur automatique sont explicitement envoyés en revue humaine (`grading_status=pending_review`), sans notation inventée. **Corrigé cette passe** : `labeling` avait un vrai comparateur serveur (`_score_assessment_response()`, `20260813140000_assessment_new_item_types.sql`) mais l'UI de passation le faisait tomber dans le textarea générique `{value}` — toujours faux, jamais `{assignments:{cible:étiquette}}`. `ItemBank.tsx` n'avait d'ailleurs aucun éditeur dédié pour les cibles/étiquettes, et `audio_video`/`file` n'avaient **aucun** bloc de réponse dans `TakeAssessment.tsx` (exclus du fallback générique mais jamais remplacés — apprenant bloqué, aucune saisie possible). Corrigés : éditeur cibles/étiquettes + crédit partiel pour `labeling`, dépôt fichier/consentement pour `audio_video`/`file` branché sur `submit_assessment_media_response()` (upload storage bucket privé `assessment-response-media` + RPC dédiée, jamais via le comparateur générique). `passage` n'avait pas d'option dans le sélecteur de type d'item (impossible à créer) — ajoutée ; rendu du stimulus dans `TakeAssessment.tsx` quand `prompt.passage` est présent. File de révision manuelle (`PendingReviewPanel`) ajoutée dans `/lms/item-bank` — `grade_assessment_response()`/`listPendingReviewResponses()` existaient côté serveur/lib sans aucun appelant UI
- [x] Rescore en masse avec prévisualisation d'impact — `preview_rescore()` liste les tentatives impactées, `execute_rescore()` recalcule les réponses auto-corrigeables, met à jour les tentatives et journalise chaque changement dans `score_adjustments` avec motif obligatoire
- [ ] Suggestions IA (génération, distracteurs, vérifications de biais/ambiguïté) — non-objectif partiel mais mentionné comme option V1
- [x] Collections/permissions granulaires — RLS applique les droits `view/use/comment/edit`, grantee lisible, création/partage/révocation exposés dans `/lms/item-bank`, collections privées/partagées/organisationnelles. **Complété cette passe** : la gestion des membres (`item_collection_members` — quels items appartiennent à la collection, condition nécessaire pour qu'une règle de tirage pool ait quoi que ce soit à tirer) n'avait ni lecture ni écriture côté UI ; ajoutée dans `CollectionsPanel`
- [x] Lien vers les questions de quiz existantes — import serveur `import_legacy_quiz_as_assessment()` + table `assessment_legacy_question_links`, accessible depuis la Banque d'items par UUID de contenu, source conservée et revisions traçables

**Incident déploiement (corrigé cette passe)** : les 5 migrations `20260813170000` à `20260813210000` (analytics spec 07, pools/import legacy, correctifs rescore/réponse) étaient commitées et leurs objets existaient réellement en base, mais absentes de `supabase_migrations.schema_migrations` — `supabase db push` échouait donc systématiquement sur la première (`analytics_daily_program` already exists) sans jamais atteindre les suivantes. Vérifié objet par objet (`information_schema`/`pg_policies`/`pg_proc`) que `170000`/`180000`/`190000` étaient intégralement appliquées avant de réparer l'historique (`supabase migration repair --status applied`) plutôt que de les rejouer ; `200000`/`210000` (uniquement `create or replace function`, idempotentes) ont été rejouées pour de vrai. `supabase migration list` confirme désormais 0 divergence local/remote sur les 110 migrations.

## 09 — Sondage live, Q&A, modération et coanimation

- [x] Écran public projeté (LIVE-015, partiel) — `/live/:code/present` (`LivePresenterScreen.tsx`), grand écran séparé de la console modérateur, classement des questions par votes en temps réel (Realtime), aucune authentification/join requis (RLS `audience_questions_public_read`, mêmes conditions que la salle participant). Limité au Q&A : sondage/priorisation/matrice n'ont toujours aucun éditeur staff (rien de réel à projeter pour ces formats)
- [x] UI d'expulsion (`kick_participant()` — bouton par participant actif dans la console animateur, `LiveEngagement.tsx::ParticipantManager`)
- [x] Répondre à un sondage (`poll`) — `open_live_interaction()`/`close_live_interaction()` (`20260812090000_live_poll_interactions.sql`) ajoutent l'invariant « un seul live par run à la fois » (auto-ferme les autres à l'ouverture) par-dessus le `submit_live_response()`/`get_my_live_response()` déjà là. Staff : création (question + options + choix simple/multiple) et tableau de résultats en direct (Realtime sur `live_responses`) dans `LiveEngagement.tsx::InteractionManager`. Participant : widget dans `LiveEventRoom.tsx` — apparaît/disparaît via Realtime sur `live_interactions`, réponse restaurée à la reconnexion, modifiable tant que le sondage reste ouvert. **Reste** : `priority`/`matrix`/`brainstorm`/`ranking` n'ont toujours ni éditeur ni lecteur (contrat config/payload différent pour chacun, pas deviné ici)
- [x] Vraie table/mécanisme d'allowlist pour `access_policy = 'allowlist'` (LIVE-002) — `20260812140000_live_event_allowlist.sql` : table `live_event_allowlist` (unicité insensible à la casse), `live_run_allowlist_ok()` ajoutée en second contrôle indépendant sur les 4 points d'entrée participant (`join_live_run`/`submit_audience_question`/`cast_vote`/`submit_live_response`) — `live_run_requires_auth()` (authentification requise) reste inchangée, l'allowlist s'y ajoute plutôt que la remplacer, no-op pour toute autre `access_policy`. UI : sélecteur de politique d'accès à la création d'un événement (jusqu'ici toujours `anonymous`, aucun moyen de choisir), `AllowlistManager` par événement (ajouter/retirer des emails) visible si `access_policy = 'allowlist'`
- [x] Mode présentateur/console modérateur distincts (LIVE-015) — pas de migration. Écran public et appareil participant étaient déjà des pages séparées ; seuls les deux rôles staff (présentateur/modérateur) restaient fusionnés dans une seule console. Scindé en onglets dans `EventRow` (`LiveEngagement.tsx`) — vues réellement distinctes, pas un panneau fusionné
- [x] Sondages sur l'écran projeté (LIVE-015) — `20260813090000_live_presenter_poll_results.sql`. `live_interactions` déjà lisible publiquement (`live_interactions_public_read`), mais `live_responses` volontairement staff-only — nouvelle RPC `get_public_live_interaction_results()` (agrégats seuls, jamais `client_id`/`payload` individuel, même posture que `get_my_live_response()`). `ProjectedPollResults` dans `LivePresenterScreen.tsx`, realtime, bascule auto sondage↔classement Q&A
- [x] Formats supplémentaires : priorisation, matrice 2×2, brainstorm, classement forcé (LIVE-009 à LIVE-013) — `20260813100000_live_brainstorm_ideas.sql` (seule migration nécessaire). `live_interactions`/`live_responses`/les 4 RPC participant (`open_live_interaction`/`close_live_interaction`/`submit_live_response`/`get_my_live_response`) sont déjà agnostiques au `kind` — zéro changement backend pour créer/répondre/fermer ces formats, seuls le contrat config/payload par format (jamais défini avant) et l'UI manquaient. Priorisation : budget de points par option. Matrice : x/y numériques par option (curseurs, pas de canvas glisser-déposer — « placement accessible » de la spec favorise un input clavier/lecteur d'écran plutôt qu'une souris). Brainstorm/texte libre (LIVE-013 fusionné dedans, pas de `kind` dédié) : idées + votes dans la même ligne de réponse (contrainte d'unicité (interaction, client) inchangée), catégories stockées dans `live_interactions.config` (déjà en écriture staff directe) plutôt qu'une nouvelle table ; nouvelle RPC `get_live_brainstorm_ideas()` — seule addition backend, nécessaire car un participant ne peut pas lire `live_responses` (pas de policy participant) donc ne peut pas voir les idées des autres pour voter sans un agrégat dédié, même raisonnement que la RPC de résultats de sondage public. Classement forcé : ordre par boutons haut/bas (même logique d'accessibilité). Comparaison avant/après (LIVE-012) : pas de nouveau mécanisme — deux interactions de classement avec la même question et `phase:'before'/'after'`, `RankingResults` les rapproche automatiquement (un seul upsert par (interaction, client) empêche de garder « avant » et « après » dans la même ligne). UI : `LiveEngagement.tsx` (formulaire de création multi-type, résultats par type), `LiveEventRoom.tsx` (widget de réponse par type). **Découverte en cours de route** : la commande `npx tsc --noEmit` utilisée pour "vérifier" tout le reste de cette session ne vérifiait en réalité **aucun fichier** (config racine composite avec `files: []` — la vraie commande est `npm run typecheck` / `tsc --noEmit -p tsconfig.app.json`). Re-vérifié tout le projet avec la bonne commande après cette découverte : 0 erreur réelle sur l'ensemble du code déjà écrit cette session — rien n'a été cassé silencieusement, seule la méthode de vérification était creuse
- [ ] Intégrations PowerPoint/Teams/Zoom (LIVE-017/018/019) — **le libellé court ci-dessus était trompeur, corrigé après lecture du texte de spec réel** (`09-live-engagement.md:92-97`) : LIVE-017 n'est pas un import de fichier .pptx — c'est un **add-in Office** (Office.js, manifest hébergé, taskpane vivant dans PowerPoint, insérer/lancer une interaction sans quitter le diaporama). L'import PPTX/PDF/Google Slides existe déjà et fonctionne (`presentation-editor/import/presentationImport.ts`, jszip + parsing OOXML côté client, `PresentationImportDialog.tsx`) mais ne répond à aucune part de LIVE-017 — fichier importé une fois, pas un add-in qui tourne dans l'appli hôte. LIVE-018 = apps Teams **et** Zoom fonctionnant *dans* l'appli hôte via leurs SDK officiels (Teams JS SDK + Bot Framework, **Zoom Apps SDK** — pas l'API REST Meetings, un autre type d'app côté Zoom, catalogue/review différents), fallback navigateur. LIVE-019 (Google Slides/Webex) explicitement hors scope V1 par la spec elle-même (« évalués après preuve de fiabilité des deux premières »). Chacun des deux chantiers réels (017, 018) est un projet app-store à part : compte développeur spécifique par plateforme (Microsoft 365 dev tenant + Azure AD pour l'add-in/bot Teams, Zoom App Marketplace pour l'app Zoom — aucun des deux n'existe dans ce projet), manifeste hébergé, soumission/review, tests possibles seulement dans le vrai client desktop. Comparable en taille à un item de §04, pas une intégration API ponctuelle — non tenté ici, nécessite d'abord la création des comptes développeur par le porteur du projet
- [x] Rapports post-session (participation, chronologie, export — LIVE-020 à LIVE-023) — pas de nouvelle migration : toutes les tables lues (`live_participants`, `audience_questions`, `live_interactions`, `live_responses`) ont déjà une policy staff (`is_live_event_staff`), composé côté client (`getSessionReport()`, `liveEngagement.ts`). LIVE-023 (« distingue absence de réponse, perte de connexion et interaction non présentée ») : `not_presented` = interaction jamais sortie de `draft` ; `connection_lost` = `last_seen_at` du participant antérieur à l'ouverture de l'interaction (seul signal honnête disponible — pas de heartbeat continu dans ce schéma, `last_seen_at` n'est rafraîchi qu'au (re)join) ; `no_response` = présent à l'ouverture, pas répondu. LIVE-021 (comparaison entre sessions) : `listEventRunSummaries()`, une ligne par run de l'événement. LIVE-022 (export anonymisé) : aucune « politique de collecte » n'existe dans le schéma pour s'y accrocher — bascule au moment de l'export à la place (« Anonymiser les participants »). Export CSV/Excel/PDF (`liveSessionReportExport.ts`, même pattern que `gradebookExport.ts`, mêmes libs déjà en dépendance — `xlsx`/`jspdf`). UI : 3ᵉ onglet « Rapport » dans `LiveEngagement.tsx::EventRow`, à côté de Présentateur/Modération
- [x] Rate limiting et filtre de termes assistant (modération) — `20260813060000_live_moderation_rate_limit_term_filter.sql`. Spec ne donne aucun seuil numérique, juste 2 contraintes : « jamais suppression invisible » et 3 dimensions « participant, appareil, événement ». Aucune infra d'empreinte appareil n'existe (aucune RPC ne prend d'id device) — `client_id` est déjà le seul proxy par-navigateur du système, participant/appareil fusionnés sur ce même identifiant plutôt que d'inventer un fingerprint ; « événement » a son propre plafond, plus large, indépendant du nombre de `client_id` distincts (défense contre de faux clients synthétiques). Table `live_event_moderation_settings` par événement (RLS `is_live_event_staff`, même précédent que `live_event_allowlist`), défauts raisonnables si absente. 3 RPC instrumentées (les seules à écrire du contenu répété sans aucune limite avant) : `submit_audience_question`, `cast_vote`, `submit_live_response` — `join_live_run` volontairement exclu (contrôle d'admission avec son propre verrou de capacité, pas un flux de contenu répété). Filtre de termes scopé à `audience_questions.body` seulement — seul contenu libre avec une porte de modération existante (`pending`/`moderate_question()`) à laquelle accrocher un flag ; `live_responses.payload` n'a aucune porte de modération et aucun format à texte libre n'est encore construit (priorisation/matrice/brainstorm/classement toujours sans éditeur). Le flag (`audience_questions.flagged_terms`) n'empêche jamais l'insertion ni l'affichage staff — juste un badge dans `QuestionModeration`, la question reste `pending` normalement, le modérateur approuve/refuse comme avant. UI : `ModerationSettingsPanel` par événement

## 10 — Gouvernance, versionnement, localisation et diffusion du contenu

- [x] Workflow de revue complet (état `in_review`/`changes_requested`/`approved`, invalidation d'approbation après modification — CNT-006/009) — `20260823010000_content_review_workflow.sql`. Les tables existaient (`content_versions` avec ses 7 états, `review_requests`, `review_steps`, `content_releases`) depuis `20260811000000_content_governance.sql` mais avaient exactement deux écrivains — `publish_content_version()`/`restore_content_version()` — tous deux sautant directement à `published` ; aucune fonction ne créait jamais de ligne `draft`, ne faisait jamais transiter vers `in_review`, n'enregistrait jamais de décision de revue, n'écrivait jamais dans `content_releases` (0 ligne possible depuis sa création). Nouveau : `save_content_draft()` (même contrat de concurrence optimiste que `publish_content_version()`, atterrit sur `draft`), `submit_content_for_review()` (`draft`→`in_review`, ouvre `review_requests`), `submit_review_decision()` (seul écrivain de `review_steps` — `approved`/`changes_requested`/`comment` ; un reviewer ne peut jamais décider sur sa propre version, séparation auteur/approbateur de CNT-007 câblée en dur plutôt qu'en option par org — la version complète, configurable, reste à faire), `publish_approved_version()` (exige `status='approved'`, seul écrivain réel de `content_releases`, canal + note de release). CNT-009 (« toute modification après approbation invalide l'approbation ») ne demandait aucune mécanique supplémentaire : `content_versions.snapshot` n'est jamais modifié après insertion, toute nouvelle édition crée une nouvelle ligne qui démarre à `draft` — l'immutabilité déjà en place suffit. `publish_content_version()` (chemin direct, sans revue — contenu personnel/solo) reste intact et fonctionne exactement comme avant, les deux chemins coexistent. CNT-008 (résolution de commentaire) : `resolveContentComment()` côté client, écriture directe — la RLS `content_comments_update` (auteur ou pedago/admin) le permettait déjà, aucune RPC nécessaire. **Reste** : CNT-007 configurable par org (aujourd'hui : séparation toujours forcée), pièces jointes/mentions sur les commentaires (CNT-008), publication/retrait programmés dans le temps (CNT-010 — canal et note de release faits, la partie planification demanderait sa propre étape de balayage nocturne pour un lecteur de `content_releases` qui n'existe encore nulle part). Vérifié : migration + 4 RPC rejouées contre un schéma stub reproduisant les vraies tables (Postgres jetable) — cycle complet brouillon→revue→changements demandés→nouveau brouillon→approuvé→publié, auto-approbation refusée pour de vrai (testée avec un pedago auteur de son propre contenu, pas seulement un rôle insuffisant), non-staff rejeté à chaque étape, double décision sur une demande déjà résolue refusée, `content_releases` reçoit sa première ligne jamais écrite, `publish_content_version()` direct toujours fonctionnel en parallèle ; `tsc`/`eslint` propres. **Non testé en conditions réelles** (pas de compte staff/apprenant local), même limite que le reste du programme
- [x] `content_deployments` réels (pinned vs follow-approved-updates, diff avant adoption — CNT-011/012) — `20260823020000_content_deployments_wiring.sql`. Vraie découverte de réconciliation avant d'écrire quoi que ce soit : la livraison de contenu en session (`course_sessions.content_snapshot`/`content_hash`/`content_schema_version`, `20260810150000_enrollment_roster.sql`) préexiste à toute la spec 10 et en est un second mécanisme de snapshot totalement indépendant — `createCourseSession()` copie `content.data` brut à la création, `content_hash` n'est même pas un vrai hash (`String(content.updated_at)`), et rien ne passe jamais par `content_versions`/`content_releases`. Cette migration ne remplace pas ce chemin — la création de session reste inchangée, fonctionne exactement comme avant pour du contenu sans release. Ce qu'elle ajoute : une couche de gouvernance **facultative** par-dessus — une fois une release publiée (via le pipeline de revue ci-dessus), le staff peut attacher un `content_deployments` à une session, et dès lors cette session peut être vérifiée contre des releases plus récentes et mise à jour explicitement. `create_content_deployment()` (vérifie que la session cible délivre bien le même contenu que la release — garde-fou contre une session non liée attachée par erreur), `check_content_deployment_update()` (diff en lecture seule : version figée vs dernière publiée, changelog, hash/schema_version changés), `adopt_content_deployment_update()` (seul écrivain de `content_deployments.pinned_version` **et** seul point qui remet à jour `course_sessions` après sa création — jamais automatique, toujours un appel explicite). CNT-013 (« une session commencée reste par défaut sur sa version ») découle gratuitement de cette discipline : rien ne tourne jamais sans être invoqué, donc une session figée ne bouge jamais toute seule. Seul `deployment_type='session'` synchronise réellement un consommateur (`course_sessions`, le seul qui existe) — `path`/`public_url`/`integration` restent créables/vérifiables mais n'ont aucune table à synchroniser aujourd'hui, non deviné. **Reste** : CNT-014 (mise à jour forcée pour faille de sécurité, avec rollback — chantier d'escalade à part) et CNT-015 (dépendances manquantes bloquent une release — demanderait un vrai graphe de dépendances entre contenus qui n'existe pas dans ce schéma). Vérifié : les 3 migrations rejouées en séquence contre un schéma stub reproduisant les vraies tables (Postgres jetable) — cycle complet release v1 → déploiement → aucune mise à jour signalée → release v2 → mise à jour détectée → session toujours figée sur v1 avant adoption → adoption explicite → session et déploiement mis à jour vers v2 → session non liée jamais touchée ; rejets vérifiés (non-staff, session appartenant à un autre contenu, version cible inexistante/non publiée) ; `tsc`/`eslint` propres. **Non testé en conditions réelles**
- [x] Modèles et blocs réutilisables (CNT-016 à 018) — `20260823050000_templates_and_reusable_blocks.sql`. Absents du modèle livré, deux concepts distincts partageant la même migration. **content_templates** (CNT-016) : contrairement aux brand kits ou aux types `path`/`public_url`/`integration` de `content_deployments`, `instantiate_content_template()` est un vrai consommateur câblé — il insère réellement dans `content`, pas une donnée orpheline. `version` auto-incrémenté par trigger uniquement quand `data` change (pas au renommage) — un signal réel, pas un champ que l'auteur peut fabriquer. **reusable_blocks/reusable_block_versions** (CNT-017) : mode « copie » = duplication ponctuelle du jsonb d'une version dans le contenu cible, rien à suivre après coup, aucune table ; mode « lien » = `block_usages` (même forme que `asset_usages`), numérotation de version race-safe par trigger (même mécanisme que `media_asset_versions`, §10 précédent). **CNT-018** (« un bloc lié signale les mises à jour ; l'adoption n'est jamais silencieuse ») scopée honnêtement : `check_block_update()`/`adopt_block_update()` miroir exact de `check_content_deployment_update()`/`adopt_content_deployment_update()` (déploiements, §10) — mais `adopt_block_update()` ne réécrit **pas** le contenu consommateur : le JSON d'un bloc lié vit à un endroit que seul le builder de ce contenu connaît (une leçon et un diaporama n'ont pas la même forme), muter ça génériquement aurait répété l'erreur déjà commise et corrigée ailleurs dans ce programme (migration des tags §03, diff de contenu plus haut dans ce même §10). L'adoption enregistre la décision explicite de l'auteur (`adopted_version`, seul écrivain, jamais appelé automatiquement) — le vrai réinsertion du contenu à jour dans le builder reste le geste de l'auteur, non deviné ici. Suppression de bloc bloquée par usage (même garde que `delete_media_asset()`, même leçon tirée : policy `for all` jamais laissée ouverte sur delete). UI : `/lms/content-library` — création de modèles/blocs (JSON brut, pas d'éditeur visuel dédié), instanciation, versions, liaison/déliaison, vérification/adoption de mise à jour, suppression guidée. Vérifié : migration + 8 RPC rejouées contre un schéma stub (Postgres jetable) — blocage RLS learner sur templates, bump de version conditionnel au changement réel de `data`, instanciation crée une vraie ligne `content`, numérotation de version race-safe sur les blocs, aucune mise à jour signalée tant que rien n'a changé puis signalée après une nouvelle version, suppression bloquée par usage puis réussie après adoption+retrait, cascade réelle ; `tsc`/`eslint` propres (seuls des avertissements `exhaustive-deps` déjà acceptés partout ailleurs dans ce codebase). **Non testé en conditions réelles**
- [x] Brand kits (CNT-019) — `20260823040000_brand_kits.sql`. Absents du modèle indicatif, table neuve `brand_kits` (couleurs jsonb, polices jsonb, logo via `media_asset_id` — réutilise la bibliothèque média déjà bâtie plutôt que dupliquer un chemin de fichier, « remplacer le logo » est alors littéralement CNT-021 gratuit) + `component_rules`/`accessibility_rules` en jsonb libre (aucun champ concret nommé par la spec, et ce codebase n'a pas de système de composants partagé à quoi accrocher un schéma fixe). Un seul kit `is_default` par org, contraint par un index unique partiel réel en base (pas une convention côté client) — `set_default_brand_kit()` bascule ancien/nouveau par défaut en une seule instruction pour ne jamais violer cet index entre deux appels. Lecture trainer/pedago/admin, écriture pedago/admin seulement (même posture que `delete_media_asset()` : l'identité de marque n'est pas à la main de chaque trainer). **Non câblé à un consommateur** : aucun builder de ce codebase ne lit un brand kit pour thémer un rendu réel — appliquer un kit à une sortie exigerait une surface themable qui n'existe pas encore, même posture que les types `path`/`public_url`/`integration` de `content_deployments` sans consommateur. Le modèle de données et le CRUD sont réels ; l'application à un rendu ne l'est pas, non deviné ici. UI : `/lms/brand-kits` — création, édition couleurs/polices/logo, prévisualisation réelle (pastilles + police + logo via URL signée), bascule défaut, suppression. Vérifié : migration + RPC rejouées contre un schéma stub (Postgres jetable) — learner/trainer rejetés en écriture par RLS directe, pedago crée, index unique partiel confirmé refuser un second défaut en écriture directe, `set_default_brand_kit()` bascule atomiquement, trainer rejeté du RPC ; `tsc`/`eslint` propres (0 avertissement). **Non testé en conditions réelles**
- [x] Gestion des assets (remplacement versionné, recherche d'usages avant suppression — CNT-020 à 023) — `20260823030000_media_asset_library.sql`. **Vraie faille trouvée avant tout autre code** : `media_assets_org` était une policy `for all` (select/insert/update/delete) sans aucun garde applicatif — un simple `.delete()` client aurait pu faire disparaître un asset utilisé ailleurs, silencieusement, dès qu'une UI l'aurait appelé (aucune UI n'existait encore, donc pas exploitée en pratique, mais la policy était déjà là, ouverte). Scindée en select/insert/update (permissif, trainer/pedago/admin) + **aucune policy delete** — `delete_media_asset()` (RPC, pedago/admin seulement, décision volontaire : un asset partagé peut être utilisé par du contenu qu'un trainer donné ne voit pas) est désormais le seul chemin de suppression, et il vérifie `asset_usages` avant de supprimer quoi que ce soit. Bucket privé `content-media-assets` (même convention que `assignment-submissions` : URLs signées 5 min, jamais d'accès direct). CNT-021 (remplacer = nouvelle version, jamais un écrasement) : `media_asset_versions.version` n'avait aucun écrivain — trigger `_set_media_asset_version_number()` calcule le prochain numéro en verrouillant la ligne `media_assets` parente (`for update`) avant de lire le max, pas un calcul côté client qui aurait pu collisionner sous upload concurrent. CNT-022 : `check_asset_deletable()` (diagnostic en lecture seule, liste les usages bloquants) + `record_asset_usage()`/`remove_asset_usage()` — **aucun scan automatique de `content.data`** pour détecter les usages : chaque builder a sa propre forme JSON, deviner un scan générique aurait été exactement l'erreur déjà commise puis corrigée ailleurs dans ce programme (migration des tags de compétences, §03) — l'auteur enregistre explicitement « j'utilise cet asset ici » depuis un sélecteur média, même posture que les preuves/alignements de compétences. CNT-023 : URLs signées faites ; scan antivirus non fait (même item déjà différé pour `submission_files.scan_status`, spec 01 — vendor à choisir, pas une deuxième question ouverte) ; quotas/types autorisés en validation client uniquement (taille max 50 Mo), aucune table de quota par org (absente du modèle indicatif aussi). UI : nouvelle page `/lms/media-library` — upload, métadonnées (licence/texte alternatif/langue), versions avec téléchargement signé, liaison/déliaison à un contenu, suppression avec la liste des usages bloquants affichée avant l'échec plutôt qu'une erreur RPC brute. Vérifié : migration rejouée contre un schéma stub avec `storage`/`content`/`media_assets` reproduisant les vraies tables (Postgres jetable) — numérotation de version race-safe confirmée, suppression bloquée tant qu'un usage existe, `.delete()` direct sous rôle `authenticated` réel refusé par RLS (0 ligne affectée, pas juste une erreur applicative), suppression réussie une fois l'usage retiré avec cascade réelle sur les versions, usage cross-org rejeté ; `tsc`/`eslint` propres. **Non testé en conditions réelles**
- [x] Localisation (L10N-001 à 005 ; L10N-006/traduction IA explicitement « facultative » dans le texte de la spec, non faite — même posture déjà tenue pour l'IA d'assistance de la spec 08) — `20260823070000_localization.sql` + `localization.ts`. **Dernier item du programme**. Le vrai défi de L10N-002 (« extraction structurée des segments, sans casser variables, formules, réponses ou mise en forme ») est un problème d'extraction générique que ce programme a refusé de deviner par type à plusieurs reprises (diff de contenu et blocs réutilisables plus haut dans ce même §10, migration des tags de compétences en §03) — `extractTranslationSegments()` utilise une **allowlist** de noms de clés (title/text/content/description/label/prompt/…), pas une denylist : un champ technique inconnu (id, discriminant de type, clé de réponse correcte, couleur hex, chemin de stockage) reste intouché plutôt que risquer une traduction qui casse le scoring ou la structure — le coût réel assumé : un champ de texte nommé autrement dans un builder non audité ne sera pas capté, limite documentée, pas silencieuse. **L10N-004** (« diff de source identifie les segments obsolètes sans effacer la traduction existante ») est la garantie de correction la plus sensible : `sync_translation_segments()` marque un segment modifié `stale` mais ne touche jamais `translated_text` ; un chemin disparu de la nouvelle extraction reste en place (orphelin, jamais supprimé) — une resynchronisation ne peut qu'ajouter ou marquer obsolète, jamais effacer le travail d'un traducteur. Aucune policy d'insertion directe sur `translation_segments` — `sync_translation_segments()` (security definer) est le seul écrivain de nouvelles lignes, la logique de réconciliation ne peut pas être répliquée par un insert client. **L10N-001** : une famille de langues par contenu (`localization_sets`, contrainte unique sur `source_content_id`). **L10N-003** : les 5 états exacts de la spec (`not_started`/`translating`/`validation`/`needs_resync`/`published`) sur `localized_versions` ; `sync_translation_segments()` rétrograde automatiquement un statut `published` vers `needs_resync` si une resynchronisation trouve des segments obsolètes — jamais l'inverse, jamais deviné pour les autres transitions. **L10N-005** : glossaires (`glossaries`, pedago/admin, lecture trainer/pedago/admin) — référence affichée au traducteur, **jamais appliqué automatiquement** (pas de moteur de substitution forcée, deviner une politique de respect automatique aurait été arbitraire) ; « validation dans la prévisualisation réelle » = `applyTranslations()` reconstruit un vrai snapshot avec chaque segment traduit substitué à son chemin réel, repli sur le texte source pour tout segment pas encore traduit (aperçu toujours lisible, jamais de trous). UI : `/lms/localization` — création de famille, ajout de langue, synchronisation, édition inline des traductions, aperçu, glossaire. Vérifié : migration + 3 RPC rejouées contre un schéma stub (Postgres jetable, **sous rôle `authenticated` réel** — un premier essai avait tourné en superuser sans le savoir sur le test glossaire, contournant silencieusement la RLS ; recorrigé et revérifié avec `set role authenticated` explicite) — learner rejeté, cycle complet insertion→traduction→resynchronisation avec source inchangée (rien ne bouge)→source modifiée (segment marqué `stale`, traduction intacte, statut version rétrogradé)→nouveau segment ajouté→segment retiré de la source laissé orphelin (jamais supprimé)→insertion directe refusée par RLS, glossaire pedago-only confirmé sous RLS réelle. Moteur d'extraction/réapplication : 22 cas via un script `tsx` autonome (suite `vitest` du dépôt toujours cassée dans cet environnement) + suite `vitest` écrite pour quand l'outillage sera réparé (`localization.test.ts`) — extraction correcte à toute profondeur, aucune fuite d'id/type/donnée de scoring, chaînes vides ignorées, réapplication avec repli sur la source, snapshot original jamais muté ; `tsc`/`eslint` propres.
- [x] Liens de preview expirants, mot de passe, filigrane, analytics minimales (PUB-004) — `20260823060000_preview_links.sql`. Le vrai enjeu de sécurité : un reviewer externe sans compte Brivia doit pouvoir ouvrir le lien, donc `resolve_preview_link()` doit être appelable par `anon` — ce qui interdit toute policy RLS `anon` directe sur `preview_links` (un select direct exposerait token/hash/expiration à quiconque) ; la table n'a **aucune** policy anon, seule la RPC security-definer résout un token, même posture que la vérification du jeton bearer SCIM/OneRoster (spec 04, `_verify_api_token()`, jamais un select direct). Mot de passe hashé avec `crypt()`/`gen_salt('bf')` (vrai bcrypt, pgcrypto) — pas le `sha256()` déjà utilisé ailleurs dans ce dépôt pour des jetons API à haute entropie : un mot de passe de lien de partage est choisi par un humain, potentiellement faible, ce n'est pas la même menace et ne doit pas partager la même fonction de hash. Raisons de rejet distinctes et stables (`not_found`/`revoked`/`expired`/`wrong_password`/`no_published_version`, même discipline que `SamlRejectReason` en spec 04) plutôt qu'un échec générique. `view_count`/`last_viewed_at` = les « analytics minimales » de la spec, incrémentées à chaque résolution réussie. UI : panneau dans `/lms/content-governance` (créer/copier/révoquer) + page publique `/preview-link/:token` (hors authentification, invite mot de passe si besoin, filigrane CSS réel si activé) — rendu **générique** (titre + dump structuré du snapshot), pas un rendu riche par type de contenu : réutiliser le vrai lecteur de chaque builder dans un contexte non authentifié et sandboxé est un projet à part, non deviné ici, même posture que le diff générique plus haut dans ce document. Vérifié : migration + 3 RPC rejouées contre un schéma stub (Postgres jetable) — learner rejeté à la création, mauvais mot de passe rejeté, bon mot de passe résout le vrai snapshot, compteur de vues réel incrémenté, lien expiré/révoqué/token inconnu chacun rejeté avec la bonne raison ; **accès direct à la table sous rôle `anon` réaliste (sans claim JWT) confirmé à 0 ligne** — un premier essai avait montré 1 ligne à tort, artefact du script de test qui n'avait pas réinitialisé le claim JWT entre deux impersonations, pas une faille réelle, recorrigé et revérifié ; `tsc`/`eslint` propres.
- [x] Export SCORM 1.2 et xAPI pour les cours (PUB-002 ; PUB-003/QTI déjà traité en spec 04, rien à faire ici) — `scormExport.ts`, aucune migration, calcul et génération de paquet entièrement côté client. Découverte de réconciliation avant tout code : `content.data` pour le type `course` est un **miroir** du `Course{title,modules,…}` de `courseStorage.ts` (localStorage — `CourseBuilder`/`CourseViewer` éditent là, pas la table `content`), confirmée par le docstring déjà existant de `duplicateContent()` — donc `content_versions.snapshot` d'un cours a réellement la forme `Course`, pas une donnée à deviner. Périmètre honnête : seules les leçons `text`/`document`/`video`/`iframe` sont du contenu statique fidèlement exportable ; `quiz`/`poll`/`flashcard`/`file-upload`/`scorm`/`h5p` exigent le moteur d'interaction/notation de Brivia — les réimplémenter dans un paquet portable indépendant de Brivia est un projet à part, non tenté ici. Chaque leçon exclue est nommée dans le rapport avec sa raison, jamais tue (même discipline que l'export QTI, spec 04). **SCORM 1.2** : vrai paquet un seul SCO — `imsmanifest.xml` réel (schéma IMS CP, `adlcp:scormtype="sco"`), `index.html` avec vraie séquence API SCORM (`findAPI()` — le pattern de référence ADL —, `LMSInitialize`/`LMSSetValue`/`LMSCommit`/`LMSFinish`), pas un stub. Séquencement multi-SCO (un SCO par module) non fait — un seul SCO avec navigation interne est la base la plus largement compatible, retenue plutôt que la sequencing complexe. **xAPI** : vrai document Activity Definition (cours + une sous-activité par leçon exportable) + modèles de statements xAPI (`initialized`/`completed`) — **pas** un lanceur cmi5 qui envoie réellement des statements à un LRS à l'exécution (exigerait la gestion réelle des paramètres de lancement cmi5 et un LRS vivant contre lequel tester, qu'aucun outil de ce dépôt ne permet de vérifier — un « lanceur qui marche » invérifiable aurait été pire que de nommer l'écart ; documenté explicitement dans le JSON exporté lui-même). UI : boutons d'export dans `/lms/content-governance` pour les contenus de type `course`, rapport affiché après export. Vérifié : 17 cas via un script `tsx` autonome (suite `vitest` du dépôt toujours cassée dans cet environnement, voir plus haut) — rapport correct (2 exportées/1 exclue avec raison nommant le type), manifest contient bien `scormtype="sco"` et le titre du cours, `index.html` appelle réellement `LMSInitialize`/`LMSSetValue`, contenu texte/vidéo intégré verbatim, leçon exclue jamais rendue comme du vrai contenu, document xAPI avec le bon nombre de sous-activités et la note explicite sur l'absence de LRS ; `tsc`/`eslint` propres.
- [x] Comparaison structurelle entre versions (diff ajouts/suppressions/déplacements — CNT-003) — `contentDiff.ts`, aucune migration : `content_versions.snapshot` porte déjà l'état complet de chaque version, rien à stocker de plus. Diff **générique**, pas par type : chaque builder de ce codebase a sa propre forme JSON sous `content.data`, deviner un schéma par type ici aurait répété exactement l'erreur déjà commise puis corrigée ailleurs dans ce programme (migration des tags de compétences §03, drag-drop/hotspot §05) — la « vue adaptée au type » de la spec n'est pas construite, la vue générique l'est. Les tableaux dont les éléments portent un `id` stable (vrai pour questions/options dans les builders quiz/exam/assessment de ce codebase) sont appariés par id plutôt que par position — un réordonnancement ressort comme un vrai déplacement (`moved`, position avant→après), pas comme N ajouts+suppressions non liés ; sans `id` stable sur les deux côtés, repli sur un diff positionnel (une insertion au milieu du tableau ressort comme une série de « changé » plutôt qu'un « ajouté » — limite documentée, pas un bug caché). UI : panneau « Comparer deux versions » dans `/lms/content-governance`, deux sélecteurs de version, liste de lignes lisibles (`+`/`−`/`~`/`⇅`). Vérifié : 14 cas (clés ajoutées/supprimées/modifiées, réordonnancement par id, ajout/suppression par id, changement imbriqué sur un item apparié, repli positionnel, changement de type sans crash, troncature d'affichage) — **suite `vitest` du dépôt cassée dans cet environnement** (`ERR_PACKAGE_PATH_NOT_EXPORTED` sur `vite`, confirmé pré-existant en lançant un test déjà existant du dépôt, pas causé par ce changement), donc vérifié via un runner `tsx` autonome important le vrai module — les 14 cas passent réellement, juste hors du harnais `npm test` habituel ; `tsc`/`eslint` propres.

## Réconciliation LMS ↔ systèmes pré-existants — items explicitement laissés de côté

- [ ] Banque d'items (spec 08, `assessment_items`) sans lien vers les questions de quiz existantes (`content.data`) — projet de migration de données à part entière
- [ ] `certificates` (clé `course_id` texte, alimenté côté client) sans lien vers la complétion d'inscription ou la maîtrise de compétences
- [ ] Incohérence de nommage `grade_items.source_type` vs `competency_evidence.source_type` — cosmétique, pas fonctionnel
- [ ] Contrôle d'accès par plan Stripe vs rôle d'organisation LMS — question de modèle économique à trancher, pas un bug

## Ordre suggéré pour la suite

1. ~~**08 — moteur de correction (`item_answer_keys`)**~~ — fait pour l'assemblage fixe + 4 types notables (voir §08). ~~Débloque la projection journalière item (07)~~ — faite aussi (voir §07) ; reste ouvert la psychométrie ANA-010/011/012 (agrégat plus riche) et, côté 08 : tirage aléatoire (pool), simulation de barème avant publication, 17 autres types d'interaction. Ne débloque pas `extra_time` (05, système `exams` séparé).
2. ~~**UI gradebook consolidée (01)**~~ — fait, y compris l'import CSV/XLSX (GBK-006, voir §01). Reste ouvert : dashboards visuels (07).
3. ~~**04 — UI admin LTI + linking**~~ — fait (voir §04) : enregistrements/déploiements/linking/diagnostic. ~~SSO OIDC/SAML général~~ — fait aussi (voir §04, `20260815040000`/`20260821000000`). ~~Deep Linking~~/~~AGS~~/~~NRPS~~ faits aussi (voir §04). ~~QTI 3~~/~~SCIM 2.0~~/~~OneRoster 1.2~~/~~API REST publique + webhooks~~/~~rotation de certificat SAML avec chevauchement~~ faits aussi. §04 est désormais clos à 13/14 — seul reste ouvert le provisioning automatique pour `sub` jamais vu (posture délibérée, pas un gap).
4. ~~**09 — écran projeté**~~ — fait pour le Q&A (voir §09). ~~Éditeur de formats sondage~~ puis ~~priorisation/matrice/brainstorm/classement forcé~~ — tous faits (voir §09). ~~Sondage sur l'écran projeté~~ — fait aussi. Reste ouvert : add-in Office (LIVE-017) + apps Teams/Zoom SDK (LIVE-018), projet app-store à part, comptes développeur inexistants — voir §09.
5. ~~**Un vrai ordonnanceur**~~ — fait (`pg_cron`, voir dépendances en tête de document). ~~Débloque la planification du balayage `release_state`~~ — branché (voir §06, avec l'évaluateur `date`). Débloque encore la *planification* des rappels J-7/J-1, de SCIM/OneRoster, des webhooks en file — mais chacun a encore besoin de sa propre logique métier avant de pouvoir être branché.
6. ~~**05 — socle accessibilité (extra_time, focus/contraste/motion, A11Y-013)**~~ — fait (voir §05, `20260815030000` + wiring `accessibility_preferences`) : spec 05 est désormais entièrement fermée. Le reste (10 localisation, 04 SCIM/OneRoster/API) peut suivre l'ordre recommandé du README du programme.
7. ~~**01 — remise fichier/audio/vidéo + URLs signées**~~ — fait (voir §01, `20260812150000`/`20260812160000` — la seconde corrige une régression introduite par la première sur le calcul de retard accommodation-aware, trouvée avant tout dégât réel puisque déployées ensemble). ~~Échéance dérogatoire par apprenant~~, ~~ciblage devoir par groupe/apprenant individuel~~ (UI seule, aucune migration), ~~connecteur antiplagiat (interface)~~, ~~notifications programmées (J-7/J-1/retard)~~ et ~~correction anonyme (GRD-005, moitié)~~ — faits aussi (`20260812180000`/`20260812220000`/`20260813010000`/`20260813020000`, voir §01). Reste ouvert dans 01 : scan antivirus (vendor à choisir, voir dépendances en tête de document), double correction (GRD-005, aucun schéma).

**Point d'arrêt 2026-08-12** — tout ce qui précède dans ce document reflète
l'état réel post-déploiement (toutes les migrations listées `[x]` sont en
prod, `supabase migration list` vérifié à chaque fois). Candidats bien
scopés pour la suite, par ordre de valeur/risque croissant :
- Tous les candidats « petits, scopés » identifiés le 2026-08-12 sont faits. Spec 06 (parcours adaptatifs) est désormais entièrement fermée, y compris son plus gros morceau (moteur d'automatisation + positionnement/remédiation, `20260813070000`-`20260813080000`) — voir §06. Spec 09 (live/sondage) ne garde ouvert que l'add-in Office PowerPoint (LIVE-017) et les apps Teams/Zoom SDK (LIVE-018) — chacun un projet app-store par plateforme, comptes développeur à créer d'abord (voir §09) — tout le reste (formats, présentateur/modérateur distincts, écran projeté, rapports, modération) est fait. Spec 05 (accessibilité) est désormais entièrement fermée aussi (`20260815030000` + wiring `accessibility_preferences` — voir §05) ; en construisant §05 il est apparu que `drag-drop`/`hotspot` (classic `questionTypes.ts`) ne sont pas de vraies interactions à rendre accessibles mais des types jamais implémentés (aucun éditeur, aucun rendu, notation qui lève explicitement) — leur reconstruction reste un chantier séparé, pas un travail d'accessibilité. Spec 04 (interopérabilité/identité) est désormais quasi close aussi (12/14 — voir §04) : SSO, LTI Advantage complet, QTI3, SCIM 2.0, OneRoster 1.2, API publique + webhooks tous faits ; seule la rotation de certificat SAML avec fenêtre de chevauchement reste ouverte, un item isolé, pas un bloc. Reste ouvert : 08 (11 types d'interaction restants, dont potentiellement `drag-drop`/`hotspot` si jamais reconstruits), 10 (gouvernance/localisation) — gros blocs non entamés ou partiels ; 01 (antivirus — vendor à choisir —, double correction — aucun schéma), 04 (rotation certif SAML), 09 (add-in Office + apps Teams/Zoom SDK).

**Point d'arrêt 2026-08-15 (session accessibilité)** — spec 05 fermée en une
session : `extra_time` (moteur de tentative chronométrée server-side,
accommodation-aware), socle focus/contraste/reduced-motion (branchement
`accessibility_preferences` → `<html data-a11y-*>` → CSS partagé, jamais fait
avant malgré les interrupteurs déjà en place), A11Y-013 (ranking rendu
accessible aux noms ; drag-drop/hotspot identifiés comme non implémentés,
pas comme un trou d'accessibilité isolé), et couverture axe-core CI étendue
aux templates skeleton + Button.

**Point d'arrêt 2026-08-22** — spec 04 fermée à 13/14 (le seul item restant,
provisioning automatique pour `sub` jamais vu, est une posture de sécurité
délibérée, pas un travail non fait) : rotation de certificat SAML avec
fenêtre de chevauchement réelle (`20260822010000_saml_cert_rotation.sql`,
voir §04) — dernier item concret ouvert du chantier. Programme LMS à 73/85
(86%). Restent des blocs non entamés/partiels : §10 (gouvernance/
localisation, vierge), §08 (11 types d'interaction), et des items isolés —
§01 (antivirus, double correction), §09 (add-in Office/Teams/Zoom SDK).

**Point d'arrêt 2026-08-23** — §10 sort de zéro : workflow de revue complet
(`20260823010000_content_review_workflow.sql`, voir §10) — draft→in_review→
approuvé→publié câblé sur les tables déjà posées le 08-11 mais jamais
alimentées (`content_releases` n'avait jamais reçu une seule ligne). Étape
suivante recommandée pour §10 : `content_deployments` réels (CNT-011/012),
seule pièce restante qui touche à des systèmes déjà en prod (sessions/
parcours) — le reste (modèles/blocs, brand kits, assets, localisation,
exports) est indépendant et peut être pris dans n'importe quel ordre.
Programme LMS à 74/85 (87%).

~~`content_deployments` réels~~ fait dans la foulée
(`20260823020000_content_deployments_wiring.sql`, voir §10) — la pièce qui
touchait à un système en prod (`course_sessions.content_snapshot`, un second
mécanisme de snapshot indépendant qui préexistait à toute la spec 10) est
faite sans casser le chemin existant : la création de session fonctionne
exactement comme avant, la couche de gouvernance est strictement optionnelle
par-dessus. §10 à 2/8. Reste ouvert, tout indépendant, n'importe quel ordre :
modèles/blocs réutilisables, brand kits, gestion des assets, localisation,
exports SCORM/xAPI/QTI, diff structurel entre versions (CNT-003). Programme
LMS à 75/85 (88%).

~~Gestion des assets~~ (CNT-020 à 023) faite aussi, même session
(`20260823030000_media_asset_library.sql`, voir §10) — vraie faille trouvée
avant tout autre code : `media_assets` avait une policy `for all` incluant
delete sans aucun garde d'usage, un simple `.delete()` client aurait pu
faire disparaître un asset utilisé silencieusement dès qu'une UI l'aurait
appelé. Scindée, suppression désormais RPC-only avec vérification d'usages.
§10 à 3/8. Reste ouvert, tout indépendant : modèles/blocs réutilisables,
brand kits, localisation, exports SCORM/xAPI/QTI, diff structurel entre
versions (CNT-003). Programme LMS à 76/85 (89%).

~~Diff structurel entre versions~~ (CNT-003) fait aussi, même session
(`contentDiff.ts`, voir §10) — aucune migration, pur calcul côté client sur
les snapshots déjà stockés. Générique, pas par type (même posture assumée
qu'ailleurs dans ce programme plutôt que deviner une forme par builder).
**Découverte en cours de route** : la suite `vitest` du dépôt est cassée
dans cet environnement (`ERR_PACKAGE_PATH_NOT_EXPORTED` sur `vite`,
confirmé pré-existant — un test déjà présent dans le dépôt échoue à
l'identique) — vérifié à la place via un runner autonome important le vrai
module (14 cas, tous verts). §10 à 4/8. Reste ouvert, tout indépendant :
modèles/blocs réutilisables, brand kits, localisation, exports SCORM/xAPI/
QTI. Programme LMS à 77/85 (91%).

~~Brand kits~~ (CNT-019) fait aussi, même session
(`20260823040000_brand_kits.sql`, voir §10) — le plus petit des items
restants, table neuve, réutilise la bibliothèque média pour le logo. §10 à
5/8. Reste ouvert, tout indépendant : modèles/blocs réutilisables,
localisation, exports SCORM/xAPI/QTI. Programme LMS à 78/85 (92%).

~~Modèles et blocs réutilisables~~ (CNT-016 à 018) faits aussi, même session
(`20260823050000_templates_and_reusable_blocks.sql`, voir §10) — le plus
gros morceau restant de §10. §10 à 6/8. Reste ouvert, tout indépendant :
localisation (L10N-001 à 006), exports SCORM/xAPI/cmi5/QTI + preview links
(PUB-002 à 004). Programme LMS à 79/85 (93%).

~~Export SCORM/xAPI + liens de preview~~ (PUB-002/004, PUB-003/QTI déjà fait
en spec 04) faits aussi, même session (`20260823060000_preview_links.sql` +
`scormExport.ts`, voir §10). §10 à 7/8 — seule la localisation
(L10N-001 à 006) reste ouverte, le plus gros morceau restant du programme
entier. Programme LMS à 80/85 (94%).

~~Localisation~~ (L10N-001 à 005 ; L10N-006/IA explicitement facultative,
non faite) faite aussi, même session (`20260823070000_localization.sql` +
`localization.ts`, voir §10) — **dernier item de §10, qui ferme la spec
entière à 8/8**. Programme LMS complet à 81/85 (95%) — restent uniquement
des items isolés déjà identifiés et documentés comme tels : §01 (antivirus
— vendor à choisir —, double correction — aucun schéma), §04 (provisioning
auto pour `sub` jamais vu — posture délibérée, pas un gap), §08 (IA
d'assistance — non-objectif partiel), §09 (add-in Office PowerPoint + apps
Teams/Zoom SDK — projet app-store à part, comptes développeur à créer
d'abord).
